# After-Release Punchlist

Items intentionally deferred to **after go-live** — enhancements, non-blocking polish, and anything that can only be exercised/verified once we're live in Production. Sibling to `Admin-Menus-Punchlist.md` (`AM-`) and `Payment-Test-Punchlist.md` (`PL-`); those two stay scoped to the pre-release verification pass.

- **Item IDs:** `AR-###`, numbered oldest → newest (newest at the bottom). The `AR-` prefix keeps this queue distinct from `AM-` / `PL-` — no number collisions, and Todd can grep `AR-` for the post-launch backlog.
- **Each item** carries Ann's request and, where known, a Claude-verified root cause (code + DB) and clear **For Todd** directions.
- **Nothing here blocks go-live.** If an item turns out to be release-critical, promote it to `AM-`/`PL-` and note the move.

---

## 🔺 OPEN FOR TODD — as of 2026-08-21

> Maintained at the top so nothing waiting on Todd is buried mid-file. **Delete a row the moment its item closes.** Parked/REVISIT items are not listed here *unless Todd owes a decision on one* (flagged as such below).

> ### ⭐ START HERE — Ann's four priority items (flagged by her, 08-22)
> **AR-021 · AR-020 · AR-019 · AR-018** — these four are the ones she wants worked. The rest of the table is the standing backlog.
>
> | | Item | One line | Why it's urgent |
> |:--|:--|:--|:--|
> | 1 | **AR-020** 🟡 FIXED 08-22 | Team RegSaver sends the **director's home address** to Vertical Insure as the event location | Unauthorized PII to a third party **and** weather payouts adjudicated against the wrong state. Live, with a real counterparty. |
> | 2 | **AR-021** 🟡 BUILT 08-23 | **Stay-to-Play admin area was never ported** — no route, no screen, no menu | **Top Threat Tournaments has paid for it and uses it.** Screen built; needs the nav re-seed + a registration for `PellucidAdmin`. |
> | 3 | **AR-019** ✅ FIXED 08-22 | TSIC-Teams password reset 404d — **client-side legacy URL, not a missing endpoint** | Fixed in the TSIC-TEAMS rewrite (Todd, publishing 08-22). No backend work. |
> | 4 | **AR-018** | ASL roster board → 2027 rollover + region changes | Seasonal deadline. Two lines of code; the rest is data. |
>
> **AR-020 and AR-021 are both promotion candidates** — neither is really "after release" material.

| Item | Why it's on Todd | State |
|:--|:--|:--|
| **AR-004** | **🔓 RE-OPENED 08-21 — was blocked on "which report", Ann has now answered it.** Report identified as `MyRosterPdfService` (**not** Packed Roster — that earlier guess was wrong, leave Packed alone). Target is one line: `MyRosterPdfService.cs:95`, `Col("Pos", 30f, …, Wrap:false)`. **Blocked only on a data query** — `SELECT DISTINCT Position` to size against the longest real value (`Long Stick Midfield`, not `Midfield`). | 🔴 OPEN |
| **AR-025** | **New 08-23 — trivial, an hour or so.** Put a **"Switch Role"** badge in the header chrome (left of home / dark-mode), calling the existing `switchRole()` (`client-header-bar.component.ts:318-322`) — same function, **same wording**, it's only reachable inside the user dropdown today. Keep the menu entry too. Watch the narrow-screen strip so it doesn't push the user menu off-screen. Ann considered and declined the "show current role instead" variant — build the literal badge. | 🔴 OPEN |
| **AR-023** | **New 08-23 — small (few hours), frontend only.** Make Payment Ledger headings sortable: date, Due, Paid; **team stays the default**. It's a hand-rolled table, not a grid, so it needs sort state + comparators + clickable `<th>`s — but every value is already on the row. **Ann decided 08-23: add a real Date column** (the date currently sits inside the Transaction cell). **🔗 Do this BEFORE AR-016** — that item says to measure the ledger's minimum width before setting the fly-in width, and the new column changes that number. **Two remaining calls are YOURS (Ann delegated 08-23)**: whether the active / waitlist-dropped buckets stay separate when sorted, and whether a sort replaces the team grouping or sorts *within* it. Defaults suggested in the item; you decide, don't wait on her. **Shared component — verify in registration detail, family-payment and club-rep-payment.** | 🔴 OPEN |
| **AR-022** | **New 08-23 — small, frontend only.** "Print this screen's help" (Help / FAQ / Pro Tips). Print stylesheet + button; no backend, no content changes. **The trap: every FAQ answer is a collapsed `<details>` and none is stored `open`, so a naive print yields questions with no answers** — force them open in `@media print`. Also un-fix the drawer (fixed overlay → static, `height:auto`), force light colours + `print-color-adjust: exact` (dark mode prints white-on-black and drops callout/highlight backgrounds), and `break-inside: avoid` on items. ~2h for the active tab; ~half a day to print all tabs as one document, which is what a director actually wants. **Whole-manual export is explicitly out of scope.** | 🔴 OPEN |
| **AR-021** | 🟡 **BUILT (Todd, 08-23) — commit `e5d4db3c`, not pushed, not deployed.** Screen ported, but **two things must happen before Ann can test**: run `scripts\5) Re-Set Nav System.sql` (seeds the STPAdmin nav row + leaf), and give `PellucidAdmin` a live STPAdmin registration on an **`ExpiryUsers`**-current job — plus change that password. **Scope shrank once legacy was read**: only **one** of the three legacy screens carried function. STPDashboard's view body is the literal text *"dashboard body here"* and nothing in the menu system links to it; STPAdminAdd is a jqGrid CRUD the Administrators page already covers and whose add path did `CreateAsync(user, password: Username)` — **that is where `PellucidAdmin`'s password came from**. Neither ported. **The filing also mis-stated the auth**: it said all three were `[Authorize(Policy="STPAdmin")]`; STPClubReps had **no `[Authorize]` at all**, and the legacy policy was `Superuser, Director, STPAdmin`, so Directors always had this. **Ann's failure was THREE missing things, not one** — the filing found the registration; STPAdmin also had **zero `nav.Nav` rows** and `RoleLookupService` never offered the role, so a registration alone would still have shown her an empty role picker. Built: `GET /api/stp/club-reps` behind a new NARROW policy `CanViewStpClubReps` (never AdminOnly — STPAdmin is a third-party vendor), job scope off the caller's registration; `GetStpAdminRegistrationsAsync` on the `ExpiryUsers` door like StoreAdmin/RefAssignor/ApiAuthorized; nav leaf in the generator beside ApiAuthorized's; ej2 grid + Excel toolbar button. **Deliberately dropped — Todd 08-23:** legacy's **batch-email half** (*"Stay-to-Play should SOLELY BE A DATA TRANSFER to a third party. I am not willing to do their work for them"*) and the rep's **home street address/city/state** that `reporting.JobClubRepContacts` carries (a housing vendor needs to reach the rep and know where the club travels from, not where they live). **Rulings:** `STPAdmin` stays its own role — an event can have both an STP vendor and a SportsRecruits vendor, likely simultaneously, and the 3rd-Party console is 0/1 login per job · grant stays on the **Administrators page**, not the console (club-rep data is adults, so the console's minors-data framing doesn't transfer). ORIGINAL: New 08-22 — not a password problem, the STP admin area was never ported. Confirmed 08-22: credentials are fine — Ann reaches role selection and gets *"No active registrations on this account"*. **Promotion candidate.** | 🟡 BUILT |
| **AR-020** | 🟡 **FIXED (Todd, 08-22) — awaiting Ann verify.** Verified end to end in the VI sandbox: quote → purchase → policy `CS-VEC-RPT-260822-0002708` ACTIVE → confirmation email, all correct. Commits `e6765609`..`f3cc7548`, pushed, **not deployed**. Event address now comes from the `*{jobPath}` field row (`EventLocationFieldNaming`, **regime B** — any attached `*` row serves, derived name wins); `location` = event state; offer **fails closed** when no complete address is on file. Four defects the filing did not know about, also fixed: `org_contact_name`/`org_contact_email` were misnamed so VI silently dropped every organization contact (legacy has the same bug); `tsic_registrationid` was always `Guid.Empty`; `customer.street` was always blank; `organization_name` and `event.name` both carried the composite `Org:Event` job name. **Deviation:** `type` left as `"Tournament"`, not `"team-registration"` — the field is `event.type` and `"team-registration"` is the VI product name, not a kind of event; spec has it as a free-form string, so neither fails. **Todd owns the data half:** 3 jobs have no field at all (`launchlacrosse-floridalaunchrising-2026`, `topthreat-merrylaxmas-north-2026`, `topthreat-bigdawgsclassic-2027`) and go dark for team insurance until an address row is added in Manage Fields. ORIGINAL:  Team RegSaver sends the **director's personal `AspNetUsers` address** to Vertical Insure as the event location (`VerticalInsureServiceTeamExtensions.cs:340-355`). **Two live harms: unauthorized PII disclosure to a third party, and weather payouts adjudicated against the wrong state.** Legacy used the field/venue bank — `FieldsLeagueSeason → Field` on LeagueId+Season — and **failed loud** ("At least one field must be attached to event") rather than substituting a person. The current director address was a deliberate workaround for a VI 400, so restore the guard too. Also realign `location` (→ event state) and `type` (→ `"team-registration"`). ~~Ask whether already-issued policies need re-issuing.~~ (Todd 08-22: not asking.) **Promotion candidate — not after-release material.** | 🟡 FIXED |
| **AR-019** | ✅ **FIXED (Todd, 08-22) — root cause was in the CLIENT, not the API.** The app never called an API for this: `login.page.ts:134` opens an in-app browser at `${WWW_URL}/tsic/Account/ForgotPassword` — the **legacy MVC page** on `www.teamsportsinfo.com`, which no longer serves. That is the 404. Fixed in the **TSIC-TEAMS rewrite**, publishing 08-22. No backend change was ever needed: `POST /api/auth/forgot-password` has existed since 2026-02-27, is anonymous, and `capacitor://localhost` is already in `Cors:AllowedOrigins` in Development, Staging and Production. Reset still lands in the **web** app (`FrontendSettings.BaseUrl`/reset-password) — a deliberate hand-off. | ✅ FIXED |
| **AR-018** | **New 08-21 — two lines of code, the rest is data.** Code: append `"ASL:Utah"` + `"ASL:New York Capital Region"` to `AslRegionConstants.Regions`; **don't remove NorCal/SoCal** (2026 + archives resolve through them). Data: clone to 2027, create Utah / NY Capital Region / California teams — **new regions, NOT renames, fresh rosters** (Ann, 08-21). Dropping NorCal/SoCal from 2027 needs no code, just no teams — **but a clone from 2026 copies them, so delete those after cloning.** Region is parsed from the team name; keep the grad year trailing. | 🔴 OPEN |
| **AR-017** | **New 08-21 — clean evidence, live bug, mechanism NOT pinned.** Red/blue set in the bulletin editor render as plain text; orange + highlight survive. **Discriminator: red/blue are exactly the hexes the legacy pipe converts to `.bl-c-*` classes (`translate-legacy-urls.pipe.ts:27-30`); orange isn't, so it stays inline.** ⇒ inline styles work, the classes don't paint — downstream of the 08-05 sanitizer fix, not it. **Deploy eliminated: images captured on current dev minutes after the re-deploy.** Sanitizer, class hook and specificity all ruled out. Start at devtools on the `$225/clinic` element + the stored `Bulletins.Text`. | 🔴 OPEN |
| **AR-016** | **New 08-21 — bigger than it first looked.** Player/team detail fly-ins open at the directive default 560px (neither sets `[defaultWidth]`) and the widening handle goes undiscovered. **Ann's confirmed spec is an outcome, not a number: no horizontal scroll on accounting, on whatever device.** Panel width fixes the desktop half only — below 768px the panel is already `100vw`, so if accounting still scrolls there, **the table has to fit, not the panel**. Measure the ledger's minimum width before picking anything; `maxWidth` 1100 may bind. Stored widths win over a new default (double-click resets). **Second time she's raised no-h-scroll (see AR-006) — give it a real answer.** | 🔴 OPEN |
| **AR-015** | **New 08-21 — two parts, split them.** **A:** bulletin Start/End time already stored (`DateTime?`) and already honored by the filter (`BulletinRepository.cs:28`, `DateTime.Now`); only the editor grid is date-only (`bulletin-editor.component.html:88-89`). Frontend-only fix — **but dates are stored in AZ time, so solve the timezone display or midnight lands on the wrong day.** **B:** time-gating a *Smart* Bulletin doesn't apply (SBs are derived from booleans, not rows) — and **this reopens AR-007 on better ground**: a scheduled window can't be "flipped once and forgotten," which was your entire objection. Needs your call on whether this is the real client need you set as the reopen bar. | 🔴 OPEN |
| **AR-014** | **New 08-21, small.** Add `EventStartDate` / `EventEndDate` to the `[reporting].[TournamentKeyAttributes-ALL]` proc so "Tournament Keys — ALL" carries tournament dates. SQL-only — the report is `kind: 'sp'`, the columns already exist on `Jobs`, the allowlist is name-only. **Proc body is not in source control** → live-DB edit; consider banking the definition. Check for null dates before shipping. **Ann added 08-21: the report also needs readable column headings** — the Excel header row is literally `reader.GetName()` on the SP's columns (`ReportingService.cs:969`), so unaliased expressions come back blank. Alias every column, including the two new dates. **One proc edit covers both asks.** | 🔴 OPEN |
| **AR-012** | **DECISION, not work.** Ann (08-21): *"I don't think we've ever had more than one league per job"* — a stronger claim than the banked data (which only checked jobs holding league-scoped fees). If no job has ever had 2+ leagues, this is **unreachable** and should close as Won't Fix rather than stay parked. One query settles it: `SELECT JobId, COUNT(*) FROM Leagues GROUP BY JobId HAVING COUNT(*) > 1`. She has seen it and is not asking for a fix. | 🔵 close-out call |
| **AR-011** | **🔓 UN-PARKED 08-21 — your own trigger condition fired.** Ann reproduced the Amount field putting a `0` back under the caret, *after* the AR-009 backdrop fix. Code confirmed unchanged (`accounting-ledger.component.ts:477`, the `?? 0`). Two inputs share `setAmount` (`:224`, `:395`) — fix once, verify both. Still no wrong-amount path, so it's friction, not risk. | 🔴 OPEN |
| **AR-013** | **Filed 08-20 — after Todd's 08-19 closure sweep, so he has very likely never seen it.** Dunning email sends expired/terminated ARB families to a "Pay Balance Due" menu item that is suppressed for them. Root cause verified; fix is a status-vs-id gate at two call sites. **No longer blocked on a decision** — the "just route everyone to Update CC" shortcut can't be validated (no terminated subscription exists to test, Ann 08-21), so build the status gate. **Promotion candidate** — misfires in Production the first time a director sends that template. | 🔴 OPEN |

**~~Also outstanding~~ — ANSWERED (Todd, 08-21): "dev was deployed."** So Ann's 08-21 verifies of AR-003/AR-006/AR-008 were against current code and stand, and AR-017 is a live bug rather than a stale bundle. Keep flagging any future "fixed" item she cannot see.

---

### Marker legend (same as the other punchlists)
- `✅ VERIFIED PASSING (Ann, MM-DD)` — Ann retested a fix, confirmed good
- `🟡 FIXED (Todd, MM-DD) — awaiting Ann verify` — fix shipped, queued for Ann's next-pass retest
- `🔴 STILL OPEN` / `🔴 RE-OPENED` — not yet fixed / regressed
- `✅ Acknowledged (Ann, MM-DD)` — Ann signed off on a decision (Won't Fix / by-design), no code change
- `Won't Fix` — decision recorded with rationale
- `🔵 REVISIT` — parked feature/enhancement, circle back

---

<!-- New items go below this line, newest at the bottom, next id = AR-026 -->

### AR-025: [Header] Surface "Switch Role" as a visible badge in the header — same function, same name, just discoverable
- **Topic**: client header bar → user chrome (left of the home / dark-mode icons)
- **Request (Ann, 08-23)**: add a small **"Switch Role"** badge to the header. **Explicitly: the same function as the existing Switch Role and the same naming** — *"just more obvious and easy for end users."*
- **📍 PLACEMENT — Ann marked two candidate spots and asked which is better (08-23).** Her screenshot boxes (a) a slot in the band **above** the app header, and (b) a slot **inline with the home / dark-mode / user-menu cluster**.
  - **Recommend (b), and specifically immediately LEFT OF the user menu ("Todd"), not at the far left of that group.** Rationale: home / dark-mode / user-menu form the session-and-account cluster, and Switch Role is an identity action — it belongs in that family. Sitting adjacent to the name reinforces what it changes (*who you are acting as*), and a user confused about which player they're on is already looking at the name.
  - **Against (a)**: the upper band is a different strip. A control placed there is divorced from the chrome it relates to and reads as page content rather than account chrome.
  - **✅ CONFIRMED (Ann, 08-23): "I like it next to the name as well."** Placement settled — **immediately left of the user menu**, inside the existing control cluster. No further decision needed.
- **Feasibility: trivial. The function already exists — this is purely surfacing it.**
  - `switchRole()` (`client-header-bar.component.ts:318-322`) just closes the menu and navigates to `/{jobPath}/role-selection`. **No new logic, no service, no backend.** The badge calls the same method.
  - Today it is reachable **only from inside the user dropdown** — desktop `client-header-bar.component.html:351`, mobile `:176` — gated on `user()?.regId`.
- **For Todd:**
  1. Add a header button labelled **"Switch Role"** (Ann's wording — keep it verbatim) calling the existing `switchRole()`. Suggested icon `bi-people-fill`, matching the menu entry it mirrors.
  2. **Gate it the same way the menu entry is gated** (`user()?.regId`) unless there's reason to do otherwise — Ann asked for the same function, so same visibility rules is the least surprising choice.
  3. **Keep the dropdown entry as well.** Two routes to one page is fine and deliberate — the badge serves people who scan, the menu serves people who hunt. Removing the menu item would be a change Ann did not ask for.
  4. **Responsive**: that strip is tight on narrow screens. Decide whether the badge collapses to icon-only or drops out below a breakpoint — it must not push the user menu off-screen on mobile.
  5. Design-system tokens only; needs a `:focus-visible` state like every other interactive element in the chrome.
- **💡 Optional, NOT the request — recorded because it came up and may be worth revisiting later.** Claude suggested the badge instead display the **current role** (`Player ▾` / `Director ▾`) and act as the switch, on the grounds that the header currently shows *who* you are but never *what* you are — the role appears only inside the dropdown as *"Signed in as **Player**"* (`:151`, `:332`). That would address orientation as well as discovery, and would help the multi-player families behind the "I don't see Update CC Info" support call (see AR-013/AR-017 territory — wrong-player confusion). **Ann considered it and chose the literal "Switch Role" badge. Build what she asked for; treat this only as a future option.**
- **Severity**: 🟢 UX / discoverability — no defect
- **Status**: 🔴 OPEN — for Todd. Filed 08-23 at Ann's request.

### AR-024: [Administrators] SuperUser needs a DIRECT Director registration — creating one via Coach Registration makes us accept waivers on a third party's behalf
- **Topic**: Configure → Administrators → Add Administrator · vs the adult/coach registration wizard
- **Request (Ann, 08-23)**: *"It would be much better to have a direct new Director registration option. Currently going through a Coach Registration is not good."* Her four reasons: a Director shouldn't have to **agree to waivers**, shouldn't have to **add a team**, may be **hard-blocked by a required USA Lacrosse number**, and — the serious one — **"if a SuperUser creates a Coach account, they are signing waivers for a third party."** Recommends a **separate registration under Add Administrator** collecting only **login / demographic / contact** details.
- **✅ ALL FOUR CONFIRMED IN CODE (Claude, 08-23).**
  - **Add Administrator is promote-only — it cannot create an account.** `AdministratorService.AddAdministratorAsync` (`:97-105`) does `FindByNameAsync` and **throws `User '…' not found`** if the person has no account. The modal's own empty-state copy says it outright: *"The person needs a coach registration under their own account — then accept them here"* and *"Register the person as a coach (any event site), then accept them here"* (`admin-form-modal.component.ts:119-155`). **The coach prerequisite is designed in, not a workaround staff invented.**
  - **Waivers** — the adult wizard has a dedicated `waivers-step.component.ts`: *"Read each waiver carefully and check to accept"*, with Accepted / Not Accepted state per waiver.
  - **Team selection is required** — `profile-step.component.ts:45-63`, gated on `state.needsTeamSelection()`: *"Required — select every team you coach."*
  - **USA Lacrosse can hard-block** — the same step renders `isUsLaxField(field)` with a verification panel, and honours `field.validation?.required`. Where a job marks USAL required, a Director with no USAL number cannot complete the form at all.
- **🚨 THE CONSENT PROBLEM IS THE REAL POINT, and it is not merely friction.** The waiver step records **legal acceptance against the registrant's account**, but the person clicking is the SuperUser. So today the only supported route to a new Director has TSIC staff **accepting waivers in another person's name**. That is a consent/compliance issue, not a UX complaint — it should be weighted accordingly when this is prioritised.
- **What a Director actually needs to exist (the minimum the new path must create):**
  1. An **`AspNetUsers` account** (username, password or invite, name, contact details).
  2. A **live `Registration` row granting Director on the job** — `bActive`, and the job unexpired on **`Jobs.ExpiryAdmin`** (admin roles ride the admin expiry lane, `RoleConstants.cs:26-33`).
  - **⚠ Both halves are required. AR-021 proved what happens with only the role**: `PellucidAdmin` holds an admin role with no live registration and gets *"No active registrations on this account"* at role selection. **Creating the user without the registration produces exactly that dead end.**
  - **Explicitly NOT collected**: waivers, team selection, fees/payment, USA Lacrosse number.
- **For Todd — decisions to make while building:**
  1. **Password vs invite** — does the SuperUser set an initial password, or does the new Director get an email invite to set their own? Invite is cleaner for consent (the Director acts for themselves) and avoids staff knowing the credential.
  2. **Which demographic fields are genuinely required** — keep it to what admin contact actually needs; do not import the coach field set by habit.
  3. **Does the same path serve other admin roles** (SuperDirector, RefAssignor, StoreAdmin, STPAdmin) or Director only? A generic "create admin account" would also unblock **AR-021**, which needs a provisioned STPAdmin.
  4. **Keep the existing promote-an-existing-user flow** — this is an *additional* path for people who have no account yet, not a replacement.
- **🔗 History**: **AR-002** surfaced this same seam — *"on Tournament/League there is no UnassignedAdult step at all… Todd to confirm whether the 'register as Coach → become Director' path is expected to exist"* — and was closed 08-19 as resolved with no work taken. **This item is the concrete follow-through on that question**, now with the consent argument attached.
- **Severity**: 🔴 **Compliance + workflow** — the current route requires staff to accept waivers on someone else's behalf, and can be outright blocked by a required USAL number
- **Status**: 🅿 **PARKED — Todd, 08-23.** *"At this stage of the rollout we’re addressing errors and consumer-based issues, not our SuperUser functionality."* The consent argument is accepted as correct; it is **not a rollout-stage priority** — nobody outside TSIC staff hits this path, and the workaround (coach registration → accept) works. Do **not** re-raise until the rollout queue turns to internal tooling. **Zero collateral:** AR-021 is explicitly not blocked by this (see AR-021 — `PellucidAdmin`’s account already exists, promote-only serves it). **Everything needed to restart cold is in this entry** — the four confirmations, the two-records requirement (`AspNetUsers` + a live Director `Registrations` row on an `ExpiryAdmin`-current job), the `ClubService.cs:169` precedent to copy, the deliberate deviation (do NOT stamp `UpdateTosAcceptanceByUserIdAsync` — leaving it unstamped makes the new Director accept the TSIC terms himself at first login), and the fact that a zero-registration account fails both arms of the existing eligibility wall so this must be a sibling write path. ORIGINAL: 🔴 OPEN — for Todd. Filed 08-23 at Ann’s request.

### AR-023: [Payments / Payment Ledger] Make the ledger column headings sortable — date, Due, Paid (keep team as the default)
- **Topic**: Payment Ledger (`accounting-ledger` component) → column headings
- **Request (Ann, 08-23)**: make the headings sortable. **Keep the current team grouping as the default — "now by team which is great"** — and add **sort by date**, plus **Due** and **Paid**.
- **Feasibility (Claude, 08-23): small — a few hours. Frontend only, no backend, no DTO.** All four sort values are already on the row: `record.date`, `record.dueAmount`, `record.paidAmount`, and the existing team key.
- **What's there now** (`accounting-ledger.component.html:30-56`, `.ts:148-180`):
  - A **hand-rolled `<table class="ledger-table">`** — *not* a Syncfusion grid, so sorting is not a config flag; it needs sort state + comparators + clickable headers.
  - Columns are **`Transaction | Due | Paid | (actions)`**.
  - Rows render in **two buckets as separate `<tbody>`s** — `activeRecords()` and `otherRecords()` (waitlist / dropped / inactive), split by a separator row.
  - Both buckets are ordered by `byTeam()`: numeric-aware `localeCompare` on the team key, **stable**, so equal teams keep their incoming chronological / `AId` order.
- **Implementation sketch**: a `sortColumn` + `sortDir` signal pair; replace `byTeam()` with a comparator switch (team = existing key; date = `record.date`; due/paid = numeric); make each `<th>` a button carrying `aria-sort` and a direction caret. Default stays `team`/asc so nothing changes until a heading is clicked.
- **⚠ THREE DECISIONS BEFORE BUILDING — ask Ann:**
  1. ~~There is no Date column to click.~~ **✅ DECIDED (Ann, 08-23): add a real Date column.** *"Adding this column won't be a problem, especially if we adjust the width of this flyin upon opening as recommended in a previous item."* The date currently lives inside the Transaction cell (`{{ record.date | date:'M/d/yy' }} · {{ paymentMethod }}`); split it out into its own sortable column.
     - **🔗 COUPLED TO AR-016 — sequence them, and mind the tension.** AR-016's spec is *no horizontal scrolling of accounting on whatever device*. **Adding a column pushes the table wider, which works against that goal**, so the two must be sized together:
       - **Do AR-023's Date column FIRST, then measure for AR-016.** AR-016 calls for measuring the ledger's minimum width before choosing a panel width — measure it against the **old** table and the number is already wrong. This ordering saves measuring twice.
       - **On small screens the extra column makes the no-scroll goal harder, not easier.** Below 768px the panel is already full viewport width, so a widened panel cannot absorb it — this pushes AR-016 further toward its "make the table itself fit" half (drop/combine columns or a compact layout at narrow widths). Worth deciding whether the Date column collapses back into the Transaction cell below a breakpoint.
  2. **Do the two buckets stay?** Sorting can apply **within** active and "other" separately (keeps waitlist/dropped from intermingling with live money) or **flatten** them into one list. *Recommendation: keep the split* — but note that sorting by date then yields two chronological runs separated by the divider, not one.
  3. **Sort within team, or replace team?** Today team is the *only* key and ties fall back to chronological. Middle option: **team first, then the chosen column inside each team** — preserves the grouping Ann likes while making dates useful. Alternative: a date sort ignores team entirely and reorders the whole table.
  - **✅ BOTH DELEGATED TO TODD (Ann, 08-23): "Todd can answer those if addressing this."** He owns 2 and 3 outright — no need to come back to her. The recommendations above are defaults, not constraints.
- **⚠ Shared component — lands in three places at once**: the registration detail panel, `family-payment`, and `club-rep-payment` all embed `<app-accounting-ledger>`. One change fixes all three, but **verify all three**, including a club rep with multiple teams (the bucket split and team keying are most visible there).
- **Severity**: 🟢 Feature / usability — no defect
- **Status**: 🔴 OPEN — for Todd. Filed 08-23 at Ann's request. **Answer the three questions above before building.**


### AR-022: [Help system] "Print this screen's help" — printable Help / FAQ / Pro Tips
- **Topic**: help drawer (`?` launcher) → print
- **Request (Ann, 08-23)**: a way to **print the help for the screen you're on** — Help, FAQ, Pro Tips. Scope is **per-screen**, not a whole-manual export.
- **Feasibility (Claude, 08-23): small job. No backend, no content changes, no schema.** Help content is already static HTML rendered into `.help-content`; this is a print stylesheet plus a button.
- **🔑 THE ONE TRAP — collapsed accordions do not print.** Every FAQ answer is a `<details class="faq-item">`, and **not one of the 57 stored `faq.html` files carries an `open` attribute** (checked 08-23). Browsers do not render the contents of a collapsed `<details>`, so a naive Ctrl+P produces **a page of questions with no answers**. Print CSS must force them open:
  ```scss
  @media print { .help-content details.faq-item > *:not(summary) { display: block !important; } }
  ```
  This is not guessable from looking at the screen — it is the single thing most likely to be missed, and the failure is silent (it looks like a clean printout).
- **Second structural issue — the drawer is a fixed overlay with its own scrollbar** (`.detail-panel.help-panel`, the shared slide-out pattern). Printed as-is it yields **one clipped screenful**. Print CSS must set the panel `position: static`, full width, `height: auto`, `overflow: visible`, and hide the app chrome (header, nav, backdrop, tab strip, launcher button, editor controls).
- **Also needed, all in the same stylesheet:**
  - **Force light colours + `print-color-adjust: exact`.** A director printing in dark mode otherwise gets white-on-black, and the callout backgrounds and the yellow highlight silently drop out.
  - **`break-inside: avoid`** on `.faq-item`, `.help-callout`, `.help-card`, `.help-menu-row` so items don't split across pages.
  - **A print header** — screen name + topic — or the printout is unidentifiable once it leaves the desk.
- **TWO TIERS — recommend doing both as one piece:**
  1. **Print the active tab.** Button + the stylesheet above. **~2 hours, mostly testing** (8 palettes, light/dark, a long FAQ that spans pages).
  2. **Print all tabs for the screen as one document** (Help + FAQ + Pro Tips together). The drawer only holds the **active** topic in the DOM and fetches per tab, so this needs the other available topics prefetched into a print-only container. **The plumbing already exists** — `HelpManifestService` knows which topics a screen has, and `HelpService.getContent` already fetches one; the launcher even caches per topic. **Small addition on top of tier 1; ~half a day for both.**
  - Tier 2 is the one a director actually wants — "print this screen's help" reading as one document rather than three separate printouts.
- **NOT in scope**: a whole printable manual across all 57 sections. That is a dedicated route with contents, ordering and section breaks — days, and worth its own conversation. **Do not let it creep in here.**
- **Note**: `pro-tips.html` exists on only **2** screens today (`ladt-editor`, `search-registrations`), so in practice most printouts will be Help + FAQ.
- **Severity**: 🟢 Feature / usability — no defect, frontend only
- **Status**: 🔴 OPEN — for Todd. Filed 08-23 at Ann's request.

### AR-021: 🟡 BUILT (Todd, 08-23) · [Stay-to-Play] STP Admin login doesn't work — the entire STP admin area was never ported. Paid feature, in live use.
- **Topic**: Stay-to-Play → STP Admin login (`STPAdmin` role)

- **🟡 BUILT 08-23 — commit `e5d4db3c`. Not pushed, not deployed. Two steps before Ann can test:**
  1. **Run `scripts\5) Re-Set Nav System.sql`** — seeds the STPAdmin `nav.Nav` row and its single leaf. Without it the role logs in to an empty menu.
  2. **Give `PellucidAdmin` a live STPAdmin registration** on a job whose **`ExpiryUsers`** is in the future (the filing said `ExpiryAdmin`; the picker actually rides `ExpiryUsers`, matching StoreAdmin / RefAssignor / ApiAuthorized — a vendor's access should not outlive the season). **And change that password.**

- **📉 THE SCOPE SHRANK ONCE LEGACY WAS ACTUALLY READ.** The filing said "rebuild the three legacy screens." Only **one** of the three carried function:
  - **STPDashboard** — `Index()` calls `GetRegKeys` then `return View()`. The view is a purple card whose body is the literal text **"dashboard body here"**, under a "Sales Rollup Pivot Table" header. It is also **unreachable**: `_STPAdminMenu` is rendered only by the dashboard's own view, and nothing in `TSICHeader` links to `STPDashboard`. Dead in legacy, not merely unported. **Nothing to port.**
  - **STPAdminAdd** — a 6-column jqGrid CRUD over STPAdmin registrations. The Administrators page already grants the role, and legacy's add path does **`CreateAsync(user, password: Username)`** with hardcoded `Gender="F"` and `Dob=1980-01-01`. **That is where `PellucidAdmin`'s password came from.** Porting it would carry the anti-pattern forward. **Not ported.**
  - **STPClubReps** — the real screen. Ported.

- **⚠️ TWO THINGS THE FILING GOT WRONG.**
  - It said all three controllers are `[Authorize(Policy = "STPAdmin")]`. **`STPClubRepsController` has no `[Authorize]` attribute at all**, and legacy `Startup.cs` sets no fallback policy — the club-rep roster and the batch-email endpoint were effectively anonymous. Not carried forward.
  - Legacy's `"STPAdmin"` policy was `Superuser, Director, STPAdmin` (`Startup.cs:131`). **Directors always had this.** The affected population was never one stranded account.

- **🔑 ANN'S FAILURE WAS THREE MISSING THINGS, NOT ONE.** The filing found the registration. Two more gates were also empty, and they are keyed differently — the registration decides which role you can *hold*; the menu is keyed by **role**, and the endpoint by **role + job**:
  1. **No registration** — the reported symptom, *"No active registrations on this account."*
  2. **No menu** — `nav.Nav` had **zero rows** for `STPAdmin` (verified against the dev DB; `ApiAuthorized` has NavId 117 with one leaf).
  3. **Role never offered at login** — `RoleLookupService` listed Superuser…ApiAuthorized but not STPAdmin.
  **So handing her a registration as a quick fix would have produced the same failure on retest**, with an empty role picker instead of an error message.

- **What was built**:
  - `GET /api/stp/club-reps` behind a new **NARROW** policy `CanViewStpClubReps` (Superuser, Director, SuperDirector, STPAdmin) — deliberately never `AdminOnly`, same reasoning as `CanRunThirdPartyExport`: STPAdmin is a third-party vendor. Job scope comes from the caller's registration, **never a route parameter**.
  - `GetStpAdminRegistrationsAsync` + `RoleLookupService` entry (role name string is the **DB** name — the JWT role claim is minted from it).
  - Nav leaf in `5) Re-Set Nav System.ps1`, beside ApiAuthorized's, so a re-seed reproduces it.
  - Angular screen at `stp/club-reps`: ej2 grid + its own **Excel Export** toolbar button; `STPAdmin` registered in `Roles`, `NAV_SERVED_ROLES`, and the display-override map (shows as *"Stay-to-Play"* at role selection).
  - Team counts bucket on agegroup name via `AgegroupConstants`, not the raw `Contains("WAITLIST")` strings legacy used.

- **✂️ DELIBERATELY DROPPED — Todd rulings 08-23, do not re-raise**:
  - **The batch-email half.** *"Stay-to-Play should SOLELY BE A DATA TRANSFER to a third party. I am not willing to do their work for them."* It does not move to the Director side either — directors already have email compose.
  - **The rep's home street address, city and state.** The existing `reporting.JobClubRepContacts` export carries them; a housing vendor needs to **reach** the rep (email, cell) and know **where the club travels from** (zip), not where they live. Same class as AR-020's director address going to Vertical Insure.
  - Legacy's `!EMAILTEMPLATE-STP-CLUBREPS` token was in the dropdown list with **no case in `ReplaceSubstitutionVariables`** — inert — and the substitution-variable picker was populated into `ViewBag` but **never rendered on the page**. Both moot now.

- **Rulings that shaped the design (do not re-litigate)**:
  - **`STPAdmin` stays its own role.** An event can have both an STP vendor and a SportsRecruits-style vendor, *"in fact, likely"* — and the 3rd-Party Data Access console is 0/1 login per job, so one login cannot be two vendors.
  - **A component, not a report.** First pass proposed the ApiAuthorized shape (report + entitlement row + nav leaf). Todd: *"why not self-contained in component?"* — correct. The vendor's registration already scopes them to one job, so the `reporting.JobReports` row was a second gate on something the login already enforces. Dropping it removed a whole piece of work.
  - **Grant stays on the Administrators page**, not the 3rd-Party console. The console's minors-data framing doesn't transfer — club-rep data is adults — and the per-job Director is the one with the STP relationship.
  - **Not blocked by AR-024.** That item cites AR-021 as needing a provisioned STPAdmin, but `PellucidAdmin`'s account already exists; promote-only is fine here. Only a *future* vendor with no account at all would need AR-024's create path.

- **Report (Ann, 08-22)**: STP Admin login doesn't work — account **`PellucidAdmin`**. **"Stay-to-Play is something Top Threat Tournaments has paid for, so this is a currently used function in the system."**
  - 🔒 **Credential deliberately NOT recorded here.** Ann supplied the password pattern in chat; it is **not** written into this file because the punchlist is committed to git. Whoever tests should get it from her directly. **If that account's password really is trivially derived from its username, change it** — it is an admin-role account on a paid customer feature.
- **✅ ROOT CAUSE: this is not a broken password. The STP admin function does not exist in the new app.**
  - **Legacy had a whole STP admin area** — `reference/.../Controllers/STP/Admin/` contains **`STPDashboardController`**, **`STPClubRepsController`** and **`STPAdminAddController`**, each `[Authorize(Policy = "STPAdmin")]`.
  - **The new app has none of it**: no STP controller, **no route** (nothing matching stp/stay in `app.routes.ts`), **no component** (no view folder), and no nav entry.
  - **The role exists but leads nowhere.** `RoleConstants.cs:21` defines `StpAdmin = CE2CB370-5880-4624-A43E-048379C64331`, name `"STPAdmin"` (`:57`), and it **is** counted in `AdminRoleIds` (`:33`). The Administrators screen can **grant** it (`administrators.component.ts:195`, `admin-form-modal.component.ts:241`) — so an admin can hand out a role that opens nothing.
  - **The frontend doesn't know the role at all.** `roles.constants.ts` `Roles` has no `StpAdmin` entry, and **`NAV_SERVED_ROLES` excludes it** — so even where authentication succeeds, the nav system serves that role no menu.
- **✅ SYMPTOM CONFIRMED (Ann, 08-22): "it simply said I had no registrations."** She adds she never suspected the password.
  - **The credentials are accepted.** That message is **"No active registrations on this account"**, rendered by the **role-selection** screen (`role-selection.component.html:106-110`) when `registrations().length === 0` (`role-selection.component.ts:35-40`). So **phase 1 of the two-phase login succeeded** and phase 2 had no role to offer. This is not an authentication failure.
  - **Why the list is empty**: STPAdmin sits in `AdminRoleIds` (`RoleConstants.cs:33`), so under the AM-004 lane model (`:26-31`) it only counts from a **live** registration — `bActive` true **and** the job unexpired on **`Jobs.ExpiryAdmin`** (admin roles ride the admin expiry, not `ExpiryUsers`). So `PellucidAdmin` either has no STPAdmin registration row, has one that is inactive, or has one on a job whose **admin expiry has passed**.
- **🔑 BOTH THINGS ARE TRUE, AND THE PORT IS THE REAL WORK.** Granting `PellucidAdmin` a live registration would clear the message and then land her **on nothing** — there is no STP route, screen or menu to reach. **Ann's read is correct: the rebuild is what's needed.** The registration gap is not the bug; it is a **prerequisite for testing the rebuild** once it exists.
- **For Todd**:
  1. **This is a feature port, not a bug fix.** Rebuild the three legacy surfaces — **STP Dashboard**, **STP Club Reps**, **STP Admin Add** — plus a nav entry and registration of `STPAdmin` in the frontend `Roles` map and `NAV_SERVED_ROLES` (it is absent from both today, so the role would get no menu even once screens exist).
  2. **Scope it against the legacy controllers** in `reference/.../Controllers/STP/Admin/` — they are the specification, and all three are `[Authorize(Policy = "STPAdmin")]`.
  3. **Before testing, provision the account**: `PellucidAdmin` needs a live STPAdmin registration on a job whose **`ExpiryAdmin`** is in the future, or the role picker stays empty regardless of what has been built.
  4. **Decide the commercial exposure**: Top Threat Tournaments has **paid** for this. If they need it before it can be ported, that is a conversation to have early rather than after they try to use it.
  4. Note the non-admin side **does** partially exist: `Jobs.BenableStp` → `JobPulse.EnableStayToPlay` (`JobRepository.cs:674`) surfaces a **Stay-to-Play** item in the user menu that routes to `store` (`client-header-bar.component.ts:181-182`). So the customer-facing entry point ships while the admin side behind it does not — confirm what that link actually reaches today.
- **Severity**: 🔴🔴 **HIGH — a paid, in-use customer feature with no admin surface in the new system.** Not a defect in ported code; an unported area discovered late.
- **Status**: 🔴 OPEN — for Todd. Filed 08-22. **Promotion candidate — a paying customer's feature being absent is not after-release material.**

### AR-020: 🟡 FIXED (Todd, 08-22) — awaiting Ann verify · [RegSaver / Team Insurance] Vertical Insure is sent the DIRECTOR'S PERSONAL ADDRESS as the event location — PII disclosure + wrong weather-payout basis
- **Topic**: Team RegSaver (Vertical Insure) → team-registration policy → `job_event.address`
- **Concern (Ann, 08-22)** — *"Major concern from a policy standpoint."* VI writes a policy against the **actual location of the event**, and **a bank of addresses was created for exactly this purpose**. Currently it appears to send a **Director's** address instead. Two harms: **(1)** it discloses personal information to VI that was never authorized for release; **(2)** it is simply wrong — a Director can live in another state, and **VI determines weather-related payouts from the event location**. *"This should be the same as it was previously."*
- **✅ CONFIRMED IN CODE — Ann is right on both counts.** `VerticalInsureServiceTeamExtensions.cs:340-355` builds the event block as:
  ```
  job_event = new VIEventDto {
      name     = jobName ?? contextName,
      type     = "Tournament",
      location = director?.OrgName ?? string.Empty,
      // "Director runs the event, so the event address is the director's
      //  address from AspNetUsers. Empty strings here cause VI's team-
      //  registration endpoint to 400 with 'Invalid zip code'."
      address  = new VIAddress {
          city   = director?.City,   state  = director?.State,
          zip    = director?.PostalCode, street = director?.StreetAddress }}
  ```
  The **director's `AspNetUsers` record** — street, city, state, postal code — is transmitted to a third-party insurer as the **event address**. The comment shows this was a **deliberate workaround for a VI 400 ("Invalid zip code")**, not an oversight, which matters: whoever fixes it must also handle the empty case that motivated it.
- **✅ LEGACY BEHAVIOUR RECOVERED — and it is exactly what Ann described.** `reference/.../IRegistrationService.cs:1022-1032` sourced the event address from the **field/venue bank**, not from any person:
  ```
  var eventKeys = await _context.FieldsLeagueSeason
      .Where(fls => fls.LeagueId == leagueId && fls.Season == regKeys.Season)
      .Select(fls => new { fls.Field.Address, fls.Field.City, fls.Field.State, fls.Field.Zip })
      .FirstOrDefaultAsync();
  ```
  then `street/city/state/zip = eventKeys.*` (`:1515-1528`). **"The bank of addresses" = the `Fields` table** (`Fields.cs` — Address, City, State, Zip, plus lat/long), joined to the job through **`FieldsLeagueSeason` on LeagueId + Season**.
- **🔑 AND LEGACY FAILED LOUD RATHER THAN SUBSTITUTING** (`:1037-1040`):
  > `if (eventKeys == null || String.IsNullOrEmpty(eventKeys.Address)) return "At least one field must be attached to event -- used for event location...";`
  **That is the answer to the 400 the current comment is working around.** Legacy's contract was *no field ⇒ no policy, tell the user to attach a field* — never *substitute a person's home address*. Restoring the field lookup **without** restoring this guard just moves the failure.
- **TWO MORE DRIFTS in the same block — fix in the same pass:**
  | Field | Legacy | Current | Effect |
  |:--|:--|:--|:--|
  | `job_event.location` | `eventKeys.State` | `director?.OrgName` | VI's "location" now carries an org name, not a place |
  | `job_event.type` | `"team-registration"` | `"Tournament"` | product-type string no longer matches what legacy sent VI |
- **For Todd**:
  1. Source `job_event.address` from `FieldsLeagueSeason → Field` (LeagueId + Season), matching legacy exactly. **Stop sending any `director.*` address field to VI.**
  2. Restore the **fail-loud guard** when no field is attached, with a message that tells the director to attach a field. Do **not** fall back to any personal record.
  3. Realign `location` → event state and `type` → `"team-registration"`.
  4. **Check the player path too**: `VerticalInsureService.cs:343-375` sends director **email, first/last name, phone, org name** as `Organization`, though `street` there is `string.Empty` (`:352`). Business contact details are defensible where a home address is not — **but confirm that is intended and authorized**, since it is the same disclosure question one product over.
  5. **Ask whether any already-issued team policies carry a director address**, and whether VI must be notified / policies re-issued. A policy written against the wrong state is a payout dispute waiting to happen, not just a data-quality nit.
- **Severity**: 🔴🔴 **HIGH — dual harm.** (a) **Personal data disclosed to a third party without authorization**, and (b) **weather payouts adjudicated against the wrong geography**, which can void or misprice a claim a club has paid for. **Promotion candidate — this is not "after release" material; it is live behaviour with a real counterparty.**
- **Status**: 🔴 OPEN — for Todd. Filed 08-22. Root cause confirmed in code, legacy behaviour recovered, fix fully specified.

### AR-019: ✅ FIXED (Todd, 08-22) · [TSIC-Teams / Mobile auth] "Reset my password" from the app login screen 404s
- **Topic**: TSIC-Teams mobile app → login screen → reset/forgot password
- **Observation (Ann, 08-22)**: before the recent mobile changes, asking to reset a password from the TSIC-Teams login screen returned a **404**. *"Not sure if this has been fixed yet."*
- **✅ ROOT CAUSE — it was never a missing endpoint. It was a hardcoded legacy URL in the CLIENT.** The earlier filing here assumed `reference/TSIC-TEAMS-2025` was empty; it is not. The shipped client is in the repo, and the button makes **no API call at all**:
  ```typescript
  // reference/TSIC-TEAMS-2025/src/app/pages/login/login.page.ts:134
  async forgotPassword() {
    await Browser.open({ url: `${WWW_URL}/tsic/Account/ForgotPassword` });
  }
  // WWW_URL (environment.prod.ts:19) = https://www.teamsportsinfo.com
  ```
  It opens an in-app browser at `www.teamsportsinfo.com/tsic/Account/ForgotPassword` — the **legacy MVC page**, which no longer serves. The 404 is an HTML 404 from a dead web route, not a route-table miss on the API. `MobileAuthController` having no reset route was a true observation but not the cause.
- **✅ FIXED in the TSIC-TEAMS rewrite** (Todd, publishing 08-22). No backend work was done and none was needed.
- **Backend was already ready** — recorded so nobody re-derives it:
  - `POST /api/auth/forgot-password` (`AuthController.cs:526-580`) has existed since `e3d54f80` (2026-02-27). It is **anonymous** (no class-level `[Authorize]`), always returns 200 regardless of match (no account enumeration), and sends one email **per matching account** — never `FindByEmailAsync`.
  - `capacitor://localhost` is already in `Cors:AllowedOrigins` in **Development, Staging and Production** — the app's webview origin can call it from every environment.
  - Sandbox gate: real email only in Production. In **Development** the response body carries the reset links (`ForgotPasswordResponse.DevResetLinks`), so the flow is walkable without a mailbox; Staging logs the link server-side only.
- **⚠ One design consequence to keep in view**: `AuthController.cs:562` builds the link as `{FrontendSettings.BaseUrl}/reset-password?token=…&userId=…` — the **Angular web app**. A TSIC-Teams user who resets leaves the app, resets in a browser, and comes back to log in. That hand-off is fine, but the app's copy should set the expectation. A mobile-prefixed endpoint (`api/mobile/auth/forgot-password`) is only worth adding if a deep-link-back-into-the-app reset is ever wanted.
- **Severity**: 🔴 was user-blocking on mobile
- **Status**: ✅ FIXED — client-side, in the rewrite. Filed 08-22, closed 08-22.


### AR-018: [ASL Rosters] Carry the roster board forward to 2027, and change the region set (NorCal→Utah, SoCal→New York Capital Region, restore California)
- **Topic**: public ASL roster board — `teamsportsinfo.com/{jobPath}/aslrosters` (screenshot: `americanselect-mainevent-2026/aslrosters`)
- **Request (Ann, 08-21)**: 1) bring rosters forward to **2027**; 2) **NorCal → Utah**, **SoCal → New York Capital Region**, and **add back California**.
- **⚠ This is mostly DATA, not code — and the two halves must land together or the board mislabels itself.**

**1) Forward to 2027 — pure job/data work, no code.**
- **Nothing in the ASL code carries a year.** Grepped `AslRostersController.cs` and the whole `views/rosters/asl-rosters/` component: **zero occurrences of 2026 or 2027**. The board is `jobPath`-scoped, so it renders whatever job you point it at.
- ⇒ Create the 2027 Main Event job (clone of `americanselect-mainevent-2026`) with its teams/rosters, and `/{new-jobPath}/aslrosters` works with no deploy.

**2) The region changes — one data half and one code half.**
- **How regions work**: region is **not a column**. It is parsed out of the team name — every ASL team is `ASL:{Region} {qualifier} {gradYear}` — by `AslRegionConstants.ResolveRegion` (`AslRegionConstants.cs:58-68`), which does a `Contains` scan over an ordered token list, first match wins.
- **The dropdown lists only regions that actually have teams**: `AslRostersController.cs:45-47` builds it as `teams.Select(ResolveRegion).Distinct()`. The constants list is a **matching vocabulary**, not the menu.
- **✅ CONFIRMED NOT RENAMES (Ann, 08-21): "they are NOT renames — simply add them new and remove NorCal and SoCal from the 2027 board."** So **Utah** and **New York Capital Region** are **brand-new regions with fresh teams and fresh rosters**. Nothing carries over from NorCal/SoCal; those two simply do not exist on 2027.
- **DATA half — create teams, don't rename any.** Name the new teams `ASL:Utah …` and `ASL:New York Capital Region …` (plus `ASL:California …`), each ending in the grad year. Until teams exist under those names, nothing appears on the board.
- **✅ "Remove NorCal/SoCal from the 2027 board" costs NO code and NO deletion of constants — it is purely an absence of teams.** The dropdown is built from teams that exist (`AslRostersController.cs:45-47`), so a region with no 2027 teams simply never appears. Leave the constants alone.
  - **⚠ BUT if 2027 is created by CLONING the 2026 job, the clone brings the NorCal and SoCal teams with it.** They must be **deleted from the 2027 job after the clone**, or they will show on the 2027 board exactly as before. This is the one step most likely to be missed.
- **CODE half — add the two new tokens** to `AslRegionConstants.Regions` (`:26-53`). **If you skip this, `ResolveRegion` matches nothing and falls back to the full team name**, so the region label and dropdown entry become e.g. `ASL:Utah Blue 2028` instead of `ASL:Utah`. Renaming without the constant is the failure mode to avoid.
- **✅ California needs NO code — it is already there.** `"ASL:California"` is the first entry in the list, present since `1383df543`. It is missing from the board only because no 2026 team is named for it. **"Add back California" = create California teams in the 2027 job**, nothing more.
- **⚠ Do NOT delete `ASL:NorCal` / `ASL:SoCal` from the constants.** They cost nothing to keep, and the 2026 job and any archived ASL job still resolve through them. Removing them would leave every historical NorCal/SoCal team falling back to its full team name.
- **⚠ Ordering is load-bearing** (stated in the file's own doc comment): first `Contains` match wins, so a token that is a substring of another must come first. Checked for this change — **`ASL:Utah` and `ASL:New York Capital Region` collide with nothing** in the existing list (`New Jersey`, `North East`, `NY Downstate`, `NY Upstate` are all distinct strings). Safe to append. **Re-check this if anyone later adds a shorter token like `ASL:New York`.**
- **⚠ Keep the grad year LAST in the team name.** `ResolveGradYear` (`:74-78`) takes the **trailing 4 characters**. `ASL:New York Capital Region Blue 2028` is fine; anything appended after the year silently breaks the grad-year grouping.
- **NET WORK — the whole item, now that it's scoped:**
  1. **Code (one file, two lines)**: append `"ASL:Utah"` and `"ASL:New York Capital Region"` to `AslRegionConstants.Regions`. Nothing else in code changes. **Do not remove NorCal/SoCal.**
  2. **Data**: create the 2027 job; create Utah, New York Capital Region and California teams (fresh rosters); **if cloned from 2026, delete the copied NorCal and SoCal teams.**
  3. **Verify**: open `/{2027-jobPath}/aslrosters` — dropdown shows Utah, New York Capital Region and California; shows **no** NorCal or SoCal; each region label reads as the region alone, not a full team name (that check is what proves step 1 landed).
  4. **Regression check**: the **2026** board still lists NorCal and SoCal correctly — proves the constants weren't stripped.
- **Severity**: 🟢 Seasonal rollover + region reconfiguration — small code change, larger data change
- **Status**: 🔴 OPEN — for Todd. Filed 08-21 at Ann's request.

### AR-017: [Communications / Bulletins] Red and blue text set in the editor renders as plain body text on the displayed bulletin
- **Topic**: Communications → Bulletins → RTE text colour → displayed bulletin card. Job: **ODU Lacrosse Camps and Clinics 2026**
- **Observation (Ann, 08-21)**: colours applied while editing don't appear once the bulletin is active. **Matched editor/display screenshots supplied for the SAME two bulletins**, so this is now a confirmed loss, not two unrelated records:
  | Text | Editor | Displayed |
  |:--|:--|:--|
  | `COST: $225/clinic` (Prospect Day) | **red** | plain body text ❌ |
  | `COST: $50` (August Youth Clinic) | **red** | plain body text ❌ |
  | `September 13th, 2026` | **blue** | plain body text ❌ |
  | `Parking is available at lot 43…` | orange | **orange** ✅ |
  | no-refunds line | yellow highlight | **yellow highlight** ✅ |
- **🔑 THE DISCRIMINATOR — it is exactly the colours the legacy pipe rewrites that fail.** `translate-legacy-urls.pipe.ts:27-30` maps **`#0000ff`/`#0000cd` → `.bl-c-blue`, `#ff0000` → `.bl-c-red`, `#000000` → `.bl-c-black`**. **Red and blue are in that map; orange is not.** So:
  - red/blue → declaration removed from `style`, replaced with a **class** → **lost on render**
  - orange + highlight → not in the map → left as **inline `style`** → **render correctly**
  - **⇒ inline styles are surviving; the `.bl-c-*` CLASSES are what fail to paint.** This is the opposite of the usual "sanitizer ate the styles" story, and it means the 08-05 sanitizer fix (`333d40ec6`) is working — the failure is downstream of it.
- **Ruled OUT (Claude, 08-21) — do not re-investigate:**
  - Sanitizer: `style` is in `GLOBAL_ATTRS` (`rich-text-sanitizer.ts:79`), `color` in `ALLOWED_CSS_PROPS` (`:119`), guard `UNSAFE_CSS_VALUE` (`:135`) doesn't reject `rgb(`. **And the orange proves inline colour reaches the page.**
  - Missing class hook: the body div **does** carry `class="bulletin-body"` (`bulletins.component.html:40`), so `.bulletin-body .bl-c-red` has its ancestor.
  - Plain specificity: `.bulletin-body .bl-c-red` (0,2,0) **beats** `.bulletin-body p { color: var(--brand-text) }` (0,1,1) at `_component-overrides.scss:243-246`, whether the class sits on the `<p>` or a `<span>` inside it. On paper the class should win.
- **⚠️ Mechanism NOT pinned — two candidates, check in this order:**
  1. **~~DEPLOY LAG~~ — FULLY ELIMINATED (Todd + Ann, 08-21).** Todd confirms *"dev was deployed"*; Ann confirms she **took these images a few minutes later, after the re-deploy**. So the screenshots show **current dev code carrying `333d40ec6`**. This is a **live bug**, the evidence is clean, and candidate 2 is the entire investigation.
     - **Corroborating, and worth stating**: a pre-`333d40ec6` build would strip inline styles and keep classes — predicting the **opposite** symptom (orange lost, red/blue surviving). Ann sees orange surviving and red/blue lost, which is only consistent with the **post-fix** build. **The evidence and the deploy timeline agree.**
  2. **PRIMARY CANDIDATE — the `.bl-c-*` rules aren't reaching the element.** They live nested under `.bulletin-body` in `_component-overrides.scss:306-308`. Confirm in devtools whether `.bl-c-red` is (a) present on the element at all — i.e. the pipe actually matched and converted — and (b) being applied or overridden. **If the RTE now emits `rgb(255, 0, 0)` rather than `#ff0000`, the map never matches**, the declaration stays inline, and it should render red — so if the element carries neither the class nor an inline colour, the loss is happening before CSS and the trail restarts at the stored HTML.
- **NEXT STEP — the one piece of data that ends this**: `SELECT Text FROM Bulletins WHERE …` for the Prospect Day bulletin on ODU Lacrosse Camps and Clinics 2026. The API stores bulletin HTML verbatim, so the stored markup shows exactly what the editor wrote for `$225/clinic` — hex vs `rgb()`, `<span style>` vs `<font color>` vs a Syncfusion class. Everything downstream is characterised; only the input shape is unknown.
- **Severity**: 🟡 Bug / authored-formatting fidelity — director sees one thing while authoring and another in production, the failure shape `333d40ec6` was written to eliminate
- **Status**: 🔴 OPEN — for Todd. Filed 08-21 with **matched, post-deploy evidence**: same two bulletins in editor and displayed, captured on current dev minutes after the re-deploy. Deploy fully eliminated as a cause. **Next: pull the stored `Bulletins.Text` for the Prospect Day bulletin and inspect the `$225/clinic` element in devtools** — is `.bl-c-red` on it, and what is overriding it? Change no sanitizer or pipe code before that.

### AR-016: [Search / Detail fly-ins] Open the player and team detail panels wide enough to show the full accounting section
- **Topic**: Search Registrations → player detail fly-in · Search Teams → team detail fly-in → accounting section width
- **Request (Ann, 08-21)**: The fly-in opens too narrow to show the full accounting section. **"Users don't know it is expandable, and almost everyone will leave it there once they know about it — so let's just have it fully expanded to begin with."**
- **✅ INTERPRETATION CONFIRMED (Ann, 08-21)** — it is the **panel width**, not a collapsed section. (Consistent with the code: `registration-detail-panel.component.html:556-566` renders `<app-accounting-ledger>` directly, ungated, and the ledger has no expand/collapse state.)
- **🎯 THE ACTUAL SPEC (Ann, 08-21), and it is not a fixed number**: *"whatever makes horizontal scrolling of accounting not necessary in whatever device is being used."* The target is **an outcome — no horizontal scroll on the accounting section — not a width.**
  - **⚠ This cannot be met by `[defaultWidth]` alone, and on small screens it may not be met by panel width at all.** The panel is capped at `maxWidth` 1100 (`resizable-panel.directive.ts:39`), and on ≤767.98px it is already forced to `width: 100vw !important` (`:23`) — i.e. **already as wide as the device allows.** If the accounting table still scrolls at full viewport width, no panel setting can fix it; **the table itself has to fit** (drop/combine columns, wrap, or a compact layout below a breakpoint). Widening the panel solves the desktop case only.
  - **So this is potentially two fixes**: (a) open wide enough on desktop that accounting fits without dragging — the easy half; (b) make the accounting table fit the viewport on smaller devices — the real half, and the one Ann's wording actually asks for.
  - **Measure first**: get the accounting ledger's natural minimum width (the point below which it scrolls), then decide whether 1100 is even sufficient, and whether `maxWidth` needs raising. Do not pick a number by eye.
  - **🔗 SEQUENCE AFTER AR-023 (Ann, 08-23).** AR-023 adds a **new Date column** to this very ledger, and Ann explicitly tied the two: *"adding this column won't be a problem, especially if we adjust the width of this flyin upon opening."* **Measuring before that column exists produces a number that is immediately stale.** Build AR-023's column first, then measure and set the width here. Note the extra column makes the no-h-scroll target *harder*, especially below 768px where the panel is already `100vw` — so it strengthens the case for the "make the table fit" half of this item (collapse the Date column back into the Transaction cell at narrow widths, or otherwise compact the layout).
  - **Note the precedent**: Ann raised a no-horizontal-scroll constraint on AR-006 too, and it was overridden there ("3rd party pushing off is perfectly fine"). **This is the second time — it is a consistent expectation, not a one-off preference.** Worth a deliberate answer rather than a second override.
- **Findings (Claude, 08-21) — the mechanism matches her description precisely:**
  - Both fly-ins use `appResizablePanel`, a drag-handle width resizer: `registration-detail-panel.component.html:5` (`storageKey="regDetailPanelWidth"`) and `team-detail-panel.component.html:5` (`storageKey="teamDetailPanelWidth"`).
  - **Neither sets `[defaultWidth]`**, so both fall back to the directive default of **560px** (`resizable-panel.directive.ts:36-38`; min 480, max 1100).
  - **Width persists in localStorage** (`:96`) — which is exactly why Ann says *"almost everyone will leave it there once they know about it."* Once widened it stays widened. The problem is purely discovery of the handle.
  - Setting an explicit default is the **established pattern**, not a new one: club rosters uses `[defaultWidth]="400"`, and both filter panels use `[defaultWidth]="520"`.
- **For Todd**:
  1. Add `[defaultWidth]` to those two panels, sized to what the accounting ledger actually needs at full width. **Do not simply max it to 1100** — on a 1366px laptop that leaves almost none of the results grid visible behind the panel, which trades one complaint for another. Measure the ledger's natural width and size to that.
  2. **⚠ The change will NOT reach anyone who has already dragged the panel.** `readStoredWidth()` prefers the stored value, so a new default only applies to users with nothing in localStorage. That's correct behaviour (never stomp a user's deliberate choice) but it means **Ann herself may not see the change** if she has ever resized it — she can double-click the handle to reset to the new default (`:18`). Say so when handing it back, or it reads as "not fixed".
  3. No mobile impact: `@media (max-width: 767.98px)` forces `width: 100vw !important` regardless of stored or default width (`:23`).
- **Severity**: 🔵 UX / discoverability — nothing is broken, but a default that hides the main content behind an undiscovered affordance costs every user the same lesson
- **Status**: 🔴 OPEN — for Todd. Filed 08-21. **Confirm the interpretation with Ann first** (panel width vs. an in-panel section).

### AR-015: [Communications / Bulletins] Bulletin Start/End need a TIME component (legacy parity) — and can that time-gate a Smart Bulletin to open/close registration?
- **Topic**: Communications → Bulletins → Start / End dates · and Smart Bulletins → registration visibility
- **Request (Ann, 08-21)**: Bulletin **Start and End dates need time added** — *"was available in Legacy… allows a bulletin to live at midnight for example."* Then the bigger question: **can that same time control be applied to a Smart Bulletin to control registration?** She ties it explicitly back to AR-007 (turning bulletins on/off) and notes **this matters to directors who want to go live or close registration at a specific time.**
- **⚠ These are TWO different problems with two different answers. Do not treat them as one.**

**PART A — director-authored bulletins: the time is ALREADY stored and ALREADY honored. Only the UI drops it.**
- `Bulletins.StartDate` / `EndDate` are **`DateTime?`**, not dates — the column has carried a time component all along. **No schema change.**
- The public filter already compares against a full timestamp: `BulletinRepository.cs:28-35` takes `var now = DateTime.Now` (**not** `.Date`) and applies `StartDate <= now` / `EndDate >= now`. **A bulletin stamped 23:00 would already stay hidden until 23:00 today.**
- **The blocker is one line of frontend**: the editor grid declares both columns date-only — `bulletin-editor.component.html:88-89`, `type="date" format="MMM d, yyyy"`. The director cannot enter a time, and a date-only picker writes midnight. **Fix = datetime picker + a format carrying time.** Frontend only; backend and DB need nothing.
- **⚠ TIMEZONE — the trap in this feature.** `BulletinRepository.cs:27` states outright: *"Bulletin start/end dates are stored in local AZ time, not UTC."* So a director in Eastern time asking for "close at midnight" would be entering **AZ time**, and a naive datetime picker will silently be 2–3 hours off. **Surfacing time without solving the timezone display is worse than date-only**, because midnight-off-by-three closes registration on the wrong day. Decide the presentation (label the field AZ/Mountain, or convert per-job) as part of the fix, not after.

**PART B — Smart Bulletins: the time control Ann wants does NOT apply, because an SB is not a row.**
- Per AR-007's finding, Smart Bulletins are **auto-derived from job flags**, not records in `Bulletins` — *"the SB sections have no durable keys, so there is nothing to hang a rule on."* There is no `StartDate` to add a time to.
- The flags an SB reads (`allowPlayerRegistration`, `allowTeamRegistration`, `allowStaffRegistration`, …) are **plain booleans** — no date dimension anywhere. So "close registration at 11:59pm" against an SB means **scheduling the flag**, which does not exist in any form today.
- **A time-window mechanism DOES already exist one level down, and it is fully time-aware**: `TeamSelfRosterAvailability.WindowContains` (`:51-55`) gates self-rostering on `Effectiveasofdate <= now && Expireondate >= now` — real timestamps, even handling sub-second insert-default pairs. Directors already close team-level registration by date/time this way (it is the documented way to reopen a past-dated job). **Worth deciding whether Ann's need is already met by that, or genuinely requires job-level flag scheduling.**

**WHY THIS REOPENS AR-007 — and on materially better ground.**
- Todd declined the SB off switch (08-19) on one specific objection: *"the kind of toggle flipped once and forgotten — a site quietly stops advertising registration while registration is open."*
- **A scheduled window is immune to that objection.** It is not a manual switch someone forgets; it **expires on its own** at a time the director chose deliberately. The failure mode Todd named cannot occur.
- It also does not undo the automation he was defending — the band still derives itself, it just respects an explicit open/close window the way every other dated thing in the system does.
- **This does not overturn his ruling** — he also recorded the correct future shape (a `smartbulletins` schema giving each SB a durable identity, *"build the schema first — do not retrofit a boolean"*). A scheduled window is exactly the sort of rule that schema is meant to hold. **Ann's request is evidence for the client need he said would be required to reopen it.**
- **For Todd — the question to answer**: does "directors want registration to open/close at a specific time" constitute the *real client need* you set as the bar for reopening AR-007? If yes, this is the first concrete use case for the `smartbulletins` schema. If no, say so and Ann has an answer.
- **For Todd — Part A is separable and should not wait on any of that.** Shipping the datetime picker for ordinary bulletins is small, honors what the DB and repository already do, and delivers the legacy parity Ann actually asked for first.
- **Severity**: 🟡 Part A = legacy-parity gap, frontend-only, timezone caveat · 🟢 Part B = feature request + a decision on AR-007's reopen bar
- **Status**: 🔴 OPEN — for Todd. Filed 08-21 at Ann's request. **Split A from B when taking it.**

### AR-014: [Reporting / X-Job Library] Add Tournament Start and End Dates to "Tournament Keys — ALL"
- **Topic**: X-Job Report Library → **Tournament Keys — ALL**
- **Request (Ann, 08-21)**: She needs a report in the X-Job Report Library carrying **tournament start and end dates**, and asks whether they can simply be added to the existing **"Tournament Keys — ALL"** rather than building something new.
- **ANSWER: yes — and it is a SQL-only change. No C#, no DTO, no API-model regen, no frontend edit.**
- **Findings (Claude, 08-21)**:
  - The report is catalog entry `xj-tournament-keys` (`x-job-report-catalog.ts:58`), declared **`kind: 'sp'`** → its entire output is whatever the stored procedure returns. Adding a column to the proc adds a column to the Excel.
  - Proc is **`[reporting].[TournamentKeyAttributes-ALL]`**, allowlisted for SuperUser cross-job execution at `GlobalSuperuserReports.cs:44`. That allowlist is name-only — it gates *who may run it*, not *what it returns*, so **the allowlist needs no edit**.
  - **The dates already exist on the Jobs table**: `Jobs.EventStartDate` and `Jobs.EventEndDate` (`Jobs.cs:212,214`, both `DateTime?`). No schema change, no new column, no backfill — the data is already there to select.
- **For Todd**:
  1. `ALTER PROCEDURE [reporting].[TournamentKeyAttributes-ALL]` to select `EventStartDate` / `EventEndDate`. **Append to the end of the column list** — Ann's users may have saved/《expected》column positions, and an SP-Excel dump has no header contract protecting mid-list insertions.
  2. **⚠ The proc body is NOT in source control.** No `.sql` file in the repo defines it (only `scripts/5) Re-Set Nav System.sql` mentions the name, for nav registration). So this is a **live-DB edit under the DB-first drill** — pull the current definition, review, apply by hand, and consider banking the definition in the repo while you're in there.
  3. **Confirm coverage before shipping**: `SELECT COUNT(*) FROM Jobs WHERE JobTypeId = 2 AND (EventStartDate IS NULL OR EventEndDate IS NULL)`. If tournaments routinely leave these null, the new columns will read blank and Ann will file this again as "the dates are empty" — better to know now whether the gap is data-entry rather than reporting.
- **ADDITION (Ann, 08-21): the report also needs COLUMN HEADINGS — "so data makes sense."** Same proc, same edit — **do both in one pass.**
  - **The Excel header row IS the SP's column names, verbatim.** `ReportingService.cs:969` builds the header from `reader.GetName(col)`, and `BuildExcelFromSheets` (`:993-995`) writes that as row 1. There is **no per-report header mapping anywhere in C#** — nothing to configure, nothing to add on the app side.
  - So a blank or cryptic heading means the proc is returning **unaliased columns**: an unaliased expression (`CASE…`, `COUNT(*)`, a concat) comes back with an **empty** name → blank header cell; a raw column comes back as the DB name (`bAllowSelfRostering`, `JobTypeId`) → technically present but not readable by a director.
  - **Fix**: alias every column in `[reporting].[TournamentKeyAttributes-ALL]` with a human-readable name (`AS [Tournament Start Date]`, `AS [Event End Date]`, …). **Alias the two new date columns the same way** — don't ship `EventStartDate` as a heading when the whole point of Ann's request is legible headings.
  - **Sanity check when done**: run the report and confirm row 1 has a readable label over **every** populated column, with no blanks. A blank header over real data is the exact symptom being reported.
- **Also worth a look while in there**: the sibling **"Job Key Attributes — ALL"** (`reporting.JobKeyAttributes-ALL`, same library, `GlobalSuperuserReports.cs:42`) may want the same two columns for non-tournament job types. Ann did not ask for it — **do not bundle it** without checking with her.
- **Severity**: 🟢 Feature / reporting enhancement (non-blocking; data exists, one proc to alter)
- **Status**: 🔴 OPEN — for Todd. Filed 08-21 at Ann's request.

### AR-013: [Payments / ARB] The "Expired/Terminated (Pay Balance Due)" dunning email sends families to a menu item they cannot see
- **Topic**: ARB Subscription Health → Behind in Payment → email template **"Expired/Terminated Subscriptions (Pay Balance Due)"**
- **What the parent sees**: the email instructs them to *"Under 'Player' in the upper right, select 'Pay Balance Due'"* (`arb-health.component.ts:57`). That menu item **is not rendered for them**. They sign in, don't find it, and call the director. This is the collections email — it lands on exactly the families who owe money.
- **Root cause (verified, Claude 08-20)** — the gate tests the wrong signal:
  - `AdnSubscriptionId` and `AdnSubscriptionStatus` are **separate columns** on `Registrations`. Expiring or terminating a plan changes the *status*; the **id is never cleared**.
  - `JobRepository.cs:1117` copies the raw column into the pulse with **no status filter** (`AdnSubscriptionId = reg.AdnSubscriptionId`).
  - Both "Pay Balance Due" surfaces suppress on **id presence**, not liveness: `client-header-bar.component.ts:139` and `registration-panel.component.ts:225`, each gated `&& !p.myAdnSubscriptionId`.
  - Net: any registration that *ever* had a subscription is treated as still-drafting forever, so the row is hidden for expired/terminated plans — the precise case the template targets.
- **The suppression itself is correct and must stay.** Both call sites carry the same comment: an ARB registrant's `OwedTotal` stays > 0 by design while the plan drafts, so a pay nudge would invite a **double payment**. The concept is right; the *condition* is wrong. It uses "has a plan id" as a proxy for "has a live plan" — identical for active plans, opposite for dead ones.
- **Intent is already documented, and the code contradicts it**: the template's own label says Pay Balance Due, and `public/help/arb-health/faq.html` explains Expired/Terminated go there *"because there's no live plan left to fix."* Help and code disagree; the help is the one that's right.
- **For Todd**: gate the "Pay Balance Due" row on subscription **status** (live = active/suspended) rather than on `myAdnSubscriptionId` being non-null. Likely wants `MyAdnSubscriptionStatus` (or a derived `MyHasLiveSubscription` bool) added to the pulse DTO alongside the id, then used at both call sites. **Two call sites — fix both**; they are independent copies of the same condition.
- **Secondary, same templates (stale vs shipped UI)**: all three ARB templates tell parents the amount is *"at the bottom of the screen"* and to *"Click Submit"* (`arb-health.component.ts:42-43, 73-74`). The shipped screen shows **Balance Due at the top** and a button reading **Update Card & Pay Balance**. The same stale wording is duplicated in `views/search/registrations/email-templates.ts:189, 212, 232, 314`.
- **~~Open question~~ — RESOLVED BY ELIMINATION (Ann, 08-21): the shortcut cannot be validated, so take the status gate.** The question was whether Update Payment Method completes for a *terminated* plan at Authorize.net — because if it did, the simplest fix would be routing **every** ARB family to "Update CC Info" and retiring the Pay Balance Due branch entirely. **Ann confirms no terminated subscription exists in the test data**, so that shortcut cannot be proven safe today. **Do not build on an unverified assumption about ADN's behavior on a dead subscription: implement the status-based gate above**, which is correct whether or not the shortcut would also have worked. Revisit the shortcut only if a terminated plan later becomes testable.
  - Related and now settled: AR-010 verified the *live*-subscription half of this flow end-to-end on 08-21, **including the same-card catch-up charge**. So the Update CC path is known-good for active/suspended plans; only the terminated case is unproven.
- **Severity**: 🔴 Bug — customer-facing, blocks self-service collections for expired/terminated ARB families. **Promotion candidate**: this misfires in Production the first time a director sends the Expired/Terminated template, so consider promoting to `PL-` rather than holding for after-release.
- **Status**: 🔴 OPEN — filed by Claude 08-20 at Ann's request. Not fixed; no code touched.

### AR-012: [Fees / Schema] A job with two leagues cannot hold per-league fees for the same role — `UX_JobFees_Scope` collapses the league tier
- **Topic**: `fees.JobFees` → league-scoped fee rows (the top tier of the base-fee cascade)
- **Origin**: found by Claude 08-19 while fixing the team-fee agegroup pin (`b2a81780`…`eb671a6e`). **Not an Ann report** — no user has hit it. Filed so it doesn't evaporate.
- **What's wrong**: `UX_JobFees_Scope` is UNIQUE on `(JobId, RoleId, AgegroupId, TeamId)`. A league-scoped row has **both** `AgegroupId` and `TeamId` NULL, and SQL Server treats NULLs as **equal** in a unique index — so only one `(JobId, RoleId, NULL, NULL)` row can exist per job per role. `LeagueId` is **not in the index**, so it cannot distinguish them. A job with two leagues therefore cannot carry a league-scoped Player fee for each; the second insert is rejected with a unique-key violation.
- **Blast radius today: ZERO — verified 08-19.** No `(job, role)` pair holds more than one league-scoped row, and no job that has league-scoped fees has more than one league. The condition has never been reachable in practice because multi-league jobs don't currently set per-league fees.
- **Why it's low priority**: it **fails loud, not silent** — a director would get a save failure, never a wrong price. Most jobs are single-league.
- **Fix when we take it**: put `LeagueId` in the key — replace `UX_JobFees_Scope` with UNIQUE `(JobId, RoleId, LeagueId, AgegroupId, TeamId)`. Schema change against a **live prod DB**, so it takes the DB-first drill: hand-written DDL, reviewed, applied by Todd. No code change needed.
- **Related, deliberately NOT bundled**: the same index also fails to constrain the **team** tier's real key `(JobId, RoleId, TeamId)` — which is what allowed the team-fee duplicate hazard fixed in `eb671a6e`. Same root shape both times: **the index describes storage columns, not what the tiers actually identify.** A redesign of the JobFees scope key would close both; neither is urgent enough to force one.
- **Severity**: latent schema defect — zero instances, fails loud, no money risk
- **ANN'S INPUT (08-21) — a STRONGER claim than the banked data, and it may close this outright**: *"I don't think we've ever had more than one league per job so not sure it's relevant. Will let Todd close this one out."*
  - **Note the difference in scope.** The 08-19 finding was narrower: no job **that has league-scoped fees** has more than one league. Ann is asserting **no job has ever had more than one league, period**. If that holds, the defect isn't merely unreached — it's **unreachable**, and this is a Won't Fix rather than a park.
  - **To settle it, one query**: `SELECT JobId, COUNT(*) FROM Leagues GROUP BY JobId HAVING COUNT(*) > 1`. Zero rows across live + legacy ⇒ close as Won't Fix on Ann's reasoning. Any rows ⇒ the park stands, and those jobs are the ones at risk the day someone sets per-league fees.
  - **Deferred to Todd for the close-out call at Ann's request** — she has seen it and is not asking for a fix.
- **Status**: 🔵 REVISIT — filed 08-19. **Awaiting Todd's close-out decision (Ann, 08-21).** Original un-park trigger still stands if it isn't closed: anyone reports a fee save failing on a multi-league job, or we take a deliberate pass at the `JobFees` scope key.

### AR-011: [Payments / Add Accounting Record] The Amount field cannot be cleared — a `0` reappears under the caret
- **Topic**: Payments → registrant/team fly-in → **Add Accounting Record** → AMOUNT field (Credit Card / Check / Correction)
- **Split out of AR-009 (08-17)**, which was the *disappearing modal* and is fixed. This is the other half of Ann's original write-up — the part about deleting the entry to retype it.
- **What the admin sees**: Amount shows `900`. Backspacing to retype gives `90`, `9`, then the moment the field goes empty **a `0` appears on its own** with the caret after it, so the new number is typed beside it and the field reads `0900`. The field fights the user.
- **Root cause (verified, Claude 08-17)**: one-way binding `[ngModel]="amount()"` + `(ngModelChange)="setAmount($event)"`, and `setAmount` coerces with `value ?? 0`. An empty number input emits `null` → stored as `0` → the signal now disagrees with the empty box, so Angular writes `0` back into it. The signal has no way to represent "empty".
- **NOT a money risk — deliberately deferred on that basis (Todd, 08-17).** `canSubmitPayment()` already refuses zero on every tab (CC needs `amt > 0`; correction returns false at `0`; check falls through to `amt !== 0`), so Save is disabled and a `$0` record cannot be booked. `0900` also parses to `900`, so even the ugly display submits the correct number. The failure mode is confusion and re-typing, not a wrong ledger amount.
- **Fix when we take it**: let the amount signal hold `null` while the field is empty and let the existing submit guard treat `null` as not-submittable. Two lines, one component, no backend, no DTO. **Money-entry binding — give it its own deliberate pass and its own verification; do NOT ride it along behind an unrelated fix.**
- **Severity**: UX friction on a money form (no incorrect-amount path)
- **🔓 UN-PARKED 08-21 — Todd's trigger condition has been met.** He parked this pending "Ann re-reports the field fighting her after the AR-009 backdrop fix lands." The backdrop fix landed (`83af23f6f`) and **Ann reproduced it deliberately on 08-21**: cursor at the end of an existing Amount, backspace to empty → **a `0` appears**. She can then type a new amount over it.
  - **Code confirmed unchanged (Claude, 08-21)**: `accounting-ledger.component.ts:475-477` still reads `this.amount.set(Math.round((value ?? 0) * 100) / 100)` — the `?? 0` is the defect — and both inputs still bind `[ngModel]="amount()"` + `(ngModelChange)="setAmount($event)"` (template `:224` and `:395`). The only commit to this component since 08-17 is the AR-009 backdrop fix, which never went near the amount box. **Nothing was fixed here; the earlier "behaves fine" was a non-repro, not a resolution.**
  - **Note two bound inputs share `setAmount`** (`:224` refund modal, `:395` add-record) — fix once, verify both.
  - **Severity confirmed unchanged**: still no wrong-amount path (submit guards refuse `0`; `0900` parses to `900`). This remains UX friction on a money form, not a ledger risk — but it is real, reproducible, and now demonstrated post-fix.
- **Status**: 🔴 OPEN — for Todd. Un-parked 08-21 on Ann's deliberate repro. Fix as scoped above (let the signal hold `null` for empty); **its own pass, its own verification — do not bundle.**

### AR-010: [Payments / ARB] Update CC for ARB subscribers — confirm it works, incl. behind-in-payments catch-up (legacy parity)
- **Topic**: Payments → ARB subscriptions → **Update CC** menu
- **Context**: This is a function we **couldn't test before but can now**. In **Legacy**, when an ARB registrant appeared under **"Look/Fall Behind in Payment"** (ARB Subscription Health → behind-in-payments list), going to the **Update CC** menu let them **make that overdue payment** at the same time — and they could **re-enter the same card** as before and still make the payment.
- **Verify — all 3 scenarios**:
  1. **Not behind in payments** → Update CC only (just swaps the card on the subscription, no charge).
  2. **Behind in payments** → Update CC **and make the past-due payment at the same time** (new card).
  3. **Behind in payments** → enter the **same CC** as before **and still make the payment** (legacy allowed re-using the identical card to catch up — confirm the new system doesn't reject "same card" and block the catch-up).
- **Test data**: **`YJ North Players 2026-2027`** currently has players who are **Behind in Payments** — use it for scenarios 2 & 3.
- **For Todd / testing**: confirm the Update CC flow (a) succeeds for the not-behind case, (b) offers/executes the catch-up charge for behind subscribers, and (c) permits the same-card re-entry catch-up. Flag if any scenario errors or silently fails to charge.
- **Severity**: Legacy-parity verification (test now that ARB behind-payment data exists)
- **Status**: ✅ VERIFIED PASSING (Ann, 08-21) — **CLOSED.** Update CC confirmed working for ARB subscribers, legacy parity held.
  - **All three scenarios confirmed (Ann, 08-21)**, including the one most at risk: **scenario 3 — re-entering the SAME card while behind still executes the catch-up charge.** Legacy allowed it; the new system does too. It does not treat an identical card as a no-op and skip the payment.
  - **Terminated plans NOT covered — no terminated subscription exists in the test data (Ann, 08-21).** So AR-013's open question cannot be answered by testing today; see the ruling recorded there.

### AR-009: [Payments / Add Accounting Record] Deleting the Amount value boots you out of the popup
- **Topic**: Payments → registrant/team fly-in → **Add Accounting Record** popup → Amount field
- **Where**: the fly-in for a **Player**, a **Team Club Rep**, or a **Team** → **Add Accounting Record** modal (Credit Card / Check / Correction tabs).
- **Repro (Ann)**: In the **AMOUNT** field, place the cursor at the **end** of an existing number and **delete the entry** (backspace) to type a different amount → the **modal closes / boots you out** unexpectedly. See image (Check tab, Amount `900` being edited, arrow on the field).
- **Likely cause (to verify, Claude)**: a **Backspace/Delete keydown on the (now-empty) Amount input is bubbling to a modal-level handler** — either an Escape/close key binding, a `keydown` that treats empty-field backspace as "back/dismiss," or the value going empty/`NaN` tripping a guard that dismisses the dialog. Classic fix: `stopPropagation` on the input's keydown (or don't close the modal on backspace/empty), and treat an empty Amount as `0`/invalid inline rather than closing.
- **For Todd**: Amount input in the Add Accounting Record modal must let the user clear/retype freely — backspace at field start/end should never close the dialog. Confirm no `@keydown`/host listener is catching Backspace, and that clearing the field doesn't trigger a close/validation-dismiss.
- **Severity**: Bug / data entry (annoying + risks losing entered check number/comment on close)
- **ROOT CAUSE — the backdrop, not the keystroke (Todd's lead, 08-17).** The filed hypothesis is wrong and worth recording as wrong: **no Backspace handler exists anywhere in the frontend** except the unrelated score-entry modal, and modal visibility has only four inbound close paths (backdrop, Escape, X, submit) — a keystroke reaches none of them. Todd's observation that clicking outside the modal dismisses it is the actual mechanism, and it was by design: the overlay carried `(click)="closePaymentModal()"`.
  - **Why a mouse wobble is enough, and why Ann's box but not Todd's:** a `click` event fires on the **nearest common ancestor of the mousedown and mouseup targets**. Press inside the card, release a few pixels outside it, and the browser dispatches `click` on the *overlay* — so the modal closes even though the press began inside it. Triggers: pointer drift (Bluetooth jitter, low battery, trackpad), clicking near the card's edge, or **`appDraggableModal` sliding the card out from under a stationary cursor**. Intermittent and hardware-sensitive by nature — which is exactly why it wouldn't reproduce on a different machine. Her mouse is the aggravating factor; the code is what turned a 3-pixel wobble into lost data entry.
- **FIXED (Claude, 08-17) — awaiting Ann verify.** Backdrop dismissal now requires the press **and** the release to land on the backdrop itself (`(mousedown)`/`(mouseup)` pair + `event.target === event.currentTarget` on both). Press-inside/release-outside and press-outside/release-inside both fall through and do nothing. Escape, X, Cancel and submit unchanged; click-away still works when it's genuinely a click away. Applies to the **CC Refund** modal too, which shares the overlay — correct, not collateral. `npx tsc --noEmit` clean.
  - Chose this over removing backdrop-close outright (also offered): it kills the failure mode without taking away a working affordance, and it holds up if her mouse is replaced tomorrow.
- **FOUND, NOT FIXED — the Amount field cannot be cleared.** `[ngModel]="amount()"` + `setAmount()` doing `value ?? 0` means deleting the last digit emits `null` → stores `0` → Angular writes `0` straight back under the caret. So "delete the entry and type a different amount" literally fights the user. Independent of the backdrop bug, and it IS the action Ann described. **Needs a go — not built.**
- **NOT SWEPT**: the same `(click)`-on-overlay backdrop pattern likely appears on other modals app-wide. Deliberately left for its own item rather than smuggled into this fix.
- **Status**: ✅ VERIFIED PASSING (Ann, 08-17) — **CLOSED.** She is no longer getting the random disappearance (Todd relayed). The press-and-release requirement holds on the machine the bug was native to, which is the only box that could have proved it. Un-park AR-011 only if she re-reports the Amount field itself fighting her.

### AR-008: [Coach Registrations / Email] Direct-placement Staff confirmation email shows no teams — "Teams you will be rostered with:" is blank on tournaments/leagues
- **Topic**: Coach Registrations → confirmation email → team list token (`!STAFFCHOICES` / `!F-TEAMS`)
- **Applies to**: **Direct-to-roster** coach type (Tournament/League Staff). **Works correctly** for the Club funnel (coach → Unassigned Adult).
- **Observation (Ann)**: On tournament/league coach confirmations, the **"Teams you will be rostered with:"** section is **empty** — the staff-teams token doesn't render the assigned teams. See image: "Ann Massey … registered as a Coach for: Long Island Elite Lacrosse:Prime Time Recruiting Showcase 2026 / Teams you will be rostered with:" then **blank** before the Contacts table. The same token **does** populate for coaches who register as Unassigned Adults.
- **Hypothesis to verify (Claude)**: the two regimes store the team selection **differently**, and the token resolver likely reads the UA shape:
  - **Unassigned Adult (Club):** selections are non-binding **REQUESTS** written into **`SpecialRequests`** (structured JSON) — the token reads from there ✅.
  - **Direct-placement Staff (Tournament/League):** `AllowTeamRequests:false`, so **nothing is written to `SpecialRequests`**; instead one **Staff `Registration` per team** is minted with **`AssignedTeamId`** set (per `e6d3bd08`). If the confirmation token resolves off `SpecialRequests` (or off a single reg's AssignedTeamId) rather than aggregating the **per-team Staff rows**, it comes back empty ❌.
  - Note there IS a `BuildStaffTeamsHtmlAsync` path that replaces `!F-TEAMS` by querying active Staff rows by user+job — worth checking whether (a) the confirmation for direct placement actually goes through that path, and (b) it runs **after** all per-team Staff rows are committed (a timing gap would also yield empty).
- **For Todd**: make the tournament/league Staff confirmation aggregate the **per-team Staff Registrations (AssignedTeamId → team)** for this user+job and render them in the "rostered with" list, matching the UA behavior. Confirm which token the template uses (`!STAFFCHOICES` vs `!F-TEAMS`).
- **Severity**: Bug / confirmation correctness (non-blocking, but the coach gets an email that omits their teams)
- **ROOT CAUSE CONFIRMED (Claude, code + data, 08-17)** — the token is **`!F-STAFFCHOICES`**, and it resolved **only** off `Registrations.specialRequests`:
  - The job's `CoachReg_ConfirmationEmail` on `lielite-primetimerecruitingshowcase-2026` reads `...Teams you will be rostered with:<br><br>!F-STAFFCHOICES...` — the heading is **director-authored per job**, not ours.
  - `BuildStaffChoicesAsync` parsed the codified `{teams:[…]}` request JSON. Direct placement sets `AllowTeamRequests: false` and **never writes that column** (per `e6d3bd08`) — the teams live in one Staff row per team with `assigned_teamID` set. No requested ids → `string.Empty` → heading over a blank. Verified against live rows on that job: `assigned_teamID` **set**, `specialRequests` **NULL/empty**, on every Staff row.
  - **Not a timing gap** and **not** `BuildStaffTeamsHtmlAsync` — that path only serves `!F-TEAMS` and already aggregates the Staff rows correctly. The bug is that the *other* team token was never taught the second regime.
  - **Scope, by effective coach template (dev-restore DB):** Tournament (2) — **144 jobs on `!F-STAFFCHOICES`** (broken) vs 212 on `!F-TEAMS` (already correct). League (3) — **12** vs 3. That per-job template split is why the symptom isn't universal.
- **FIXED (Claude, 08-17) — awaiting Ann verify.** Fixed in the **shared resolver** (Todd's pick over pre-rendering at the adult chokepoint, so every consumer of the token benefits, not just the adult wizard):
  - `BuildStaffChoicesAsync` — when SpecialRequests yields no requested ids, fall back to `GetCoachTeamChoicesAsync` (every **active** Staff row for this user+job, one per team) and render the same `Club: Age: Team` list. **UA path byte-identical** — with requested ids present nothing changes. Dedupes on the label: the email lists teams, not rows.
  - `GetCoachTeamChoicesAsync` — two latent defects hardened in the same pass, both live only now that real confirmations route through it: added the **`bActive` filter** (a coach removed from a team must not see it on a resend), and the club-rep join for the club name is now a **left join** with a null-safe projection (that FK is nullable — a rostered team must never drop out of a confirmation for lacking a club rep; mirrors `GetTeamLabelsByIdsAsync`).
  - Backend only — no DTO change, no API-model regen. `dotnet build` green.
- **Rulings recorded**: coaches already mailed a blank list get **no resend and no backfill** (Todd) · the *"Teams you will be rostered with:"* heading **stays as authored** (Todd). Noted but NOT actioned: the same token on a **club** site renders *requested* teams under a heading promising placement — a per-job copy mismatch across ~323 club templates, a separate item if it ever matters.
- **Status**: ✅ VERIFIED PASSING (Ann, 08-21) — **CLOSED.** Tournament/league coach confirmation now lists every team; club-site "Requested Teams" path unchanged. Noted but still unfiled: the same token on a **club** site renders *requested* teams under a heading promising placement (~323 club templates) — separate item if it ever matters.

### AR-007: [Communications / Smart Bulletins] Allow SuperUser (and consider Director) to inactivate a Smart Bulletin
- **Topic**: Communications → Smart Bulletins (the auto-generated registration banner / quicklinks)
- **Request (Ann)**: Give **SuperUser** the ability to **inactivate (turn off) a Smart Bulletin** when they don't want it shown. **Consider extending the same option to Directors.**
- **Why (Ann's use case)**: A director may prefer a **customized, info-only banner** (announcement text without the registration quicklinks), or may **not want the Smart Bulletin to show at all**. Right now there's no way to suppress it, so they can't substitute their own banner or hide it.
- **For Todd — decisions**:
  1. **Scope** — SuperUser-only first, or SuperUser + Director? (Ann leans toward offering it to Directors too.)
  2. **Granularity** — a simple **on/off (active/inactive) toggle** per Smart Bulletin, vs. an "info-only" mode that keeps the banner but strips the registration quicklinks. Ann's phrasing suggests both are desirable (turn off entirely *or* show info without quicklinks).
  3. Confirm what "Smart Bulletin" maps to in the current model (auto-generated vs. director-authored bulletin) so the toggle lands on the right entity.
- **Severity**: Feature / director control (non-blocking)
- **MODEL ANSWER to Ann's Q3 (Claude, 08-19): a "Smart Bulletin" is NOT a record.** The ✨ SMART band is assembled at render time from the live pulse (`smart-bulletins.component.ts`); each section is a template branch gated on its pulse flag **and** the lifecycle phase. No row, no id, no Active column — *"the smart panels are read-only for all."* The hand-authored bulletins are the ones with rows, and they **already** carry a one-click inactivate for `isAdmin()` = Director/SuperDirector/SuperUser (`bulletins.component.ts:82`), so Ann's "SuperUser first, Directors maybe" is already answered in the more generous direction on the surface that has rows.
- **RULING 1 — suppression DECLINED (Todd, 08-19): "we built this for a reason, to alleviate club rep admin time, why the hell are we undoing that?"** A director-facing off switch undoes the automation the band exists to provide, and is the kind of toggle flipped once and forgotten — a site quietly stops advertising registration while registration is open. It also kills the dashboard's premise (the director sees exactly what the public sees). Same shape as the **AM-091** ruling: an auto-derived card is a *true readout*, not papered over. **Never re-propose an SB off switch.**
- **RULING 2 — reordering (bulletins above the band) REJECTED ON DATA (08-19).** Looked free (order is pure template — `bulletins.component.html` emits the band, then the "News & Updates" divider, then the manual `@for`; nothing data-driven). **But of 297 live bulletins across 187 jobs, 294 started >30 days ago and 269 started >180 days ago.** They are in-window because of a far-out EndDate, not because they are current — so flipping would put *last season's notice* above the live registration CTA and the game clock on 187 sites. The band also **self-hides when empty**, so SB-first never crowds a director without cause, while B-first pushes the band down unconditionally. The "it's the organizer's site, their voice leads" framing also optimizes for the organizer over the visitor, who came to register / check a schedule / find a game time — all of which live in the band.
- **RULING 3 — per-bulletin "pin above the band" REJECTED as a cosmetic schema change (Todd, 08-19).** A new `Jobs.Bulletins` column for landing-page ordering is presentation, not data, on a live prod DB shared with staging and legacy — and the freshness numbers say nobody is publishing timely notices they need to elevate. **Do NOT launder `BCore` or `ExpireHours` into this**: `BCore` is scaffolding-only (mapped in the DbContext of BOTH the new stack and the legacy TSIC-Unify reference, **never read or written by any code**; 3,474 true / 1 false / 470 null = an insert default, not a decision).
- **📌 THE CORRECT FUTURE APPROACH — recorded now, deliberately NOT built (Todd, 08-19): "this is just not the time to do it."** The blocker was never storage, it was **identity**: the SB sections have no durable keys, so there is nothing to hang a rule on. The right shape is a **new `smartbulletins` SQL schema** (a schema namespace, sibling to `Leagues`, `Jobs`, `widgets`, `nav` — *not* "a schema change" in the loose sense) that **tracks the individual SB types as rows** and **allows overrides** against them. That gives each smart bulletin a first-class identity, at which point suppression / precedence / per-job and per-role overrides all become ordinary rows instead of columns bolted onto `Jobs` or `Jobs.Bulletins`. **When this is picked up, build the schema first — do not retrofit a boolean.** (For reference: `widgets.JobWidget` already models a per-job/per-role override tier with `IsEnabled` + `Config`, but it is **empty** — 0 rows — and the band is hard-placed in `job-landing.component.html:72`, never routed through the widget outlet, so the widget tables are NOT a shortcut here.)
- **Status**: ✅ CLOSED (Todd, 08-19) — suppression declined on principle, reorder rejected on data, pin rejected as cosmetic schema. **Reopen only with a real client need**, and then via the `smartbulletins` schema above. ✅ Acknowledged (Ann, 08-21).

### AR-006: [LADT] Surface "Self Roster" state in LADT — Age Group column + info buttons; clarify vs Team "Active"
- **Topic**: LADT (League / Age-Group / Division / Team editor) → self-rostering visibility
- **Request (Ann)**:
  1. **Age Group level** — add a **SELF ROSTER** column (checked / unchecked) so a director can see at a glance whether self-rostering is turned on for that age group.
  2. **Team level clarity** — the Team editor shows **Active**, but it's ambiguous: does **Active** mean self-rostering is on, or is **Self Rostering a separate toggle** that also has to be turned on? Make the distinction explicit.
  3. **Info buttons on both levels** — Ann assumes self-roster is **hierarchical** (set at a higher level, inherited down, like the payment-phase cascade). Add an **info (ℹ) button at both the Age Group and Team levels** noting **where the setting is actually controlled** (which level owns it, what's inherited).
- **For Todd — confirm the model first**: pin down the real self-roster flag(s) and their hierarchy before building the UI — is there an age-group-level self-roster flag, a team-level one, both, and how do they cascade? **⚠ Roster-visibility semantics were just changed in `e6d3bd08` (consent-gated `BAllowRosterViewAdult`)** — confirm whether "self roster" here is that same flag or a distinct player-self-rostering toggle, so the new column/info copy describes the right thing. Use design-system info-button pattern; no hardcoded colors.
- **⚠ Constraint (Ann)**: adding the SELF ROSTER column **must NOT introduce horizontal scrolling** on the Age Group grid. Fit it within the existing width (tighten/absorb other columns, use a compact checkbox/icon cell) — no h-scroll.
- **Severity**: UI clarity / director usability (non-blocking)
- **MODEL CONFIRMED (Claude, 08-19) — Ann's hierarchy assumption is WRONG.** `Agegroups.bAllowSelfRostering` and `Teams.bAllowSelfRostering` are **OR'd, not a cascade** (`TeamSelfRosterAvailability.cs:39`) — neither level overrides the other; an age-group check is a *floor* enabling every team under it regardless of that team's own unchecked box. `Active` is **necessary but not sufficient**: the real rule is Active **and** a self-roster flag **and** not a WAITLIST/Dropped bucket **and** inside the team's `Effectiveasofdate`/`Expireondate` window (null/zero-width window PASSES). **The ⚠ is resolved: this is NOT the `e6d3bd08` flag** — `BAllowRosterViewAdult` governs who may *view* a roster; `bAllowSelfRostering` governs whether a player may *add themselves*. Different tables, different purpose.
- **Data (SQL, 08-19): the age group IS the answer.** Of 51,411 teams, **44,249** are self-rosterable via their age group's flag and **exactly ONE** is enabled by a team-level checkbox alone (`stepscalifornia-players-2026-2027`, agegroup `Summer Season ONLY`, live). The 861 team-level flags are 26 jobs / ~2 customers (LI Yellow Jackets, TLC Lacrosse) propagating via clone `CopyRosterSettings`. Safe to build **display** on the age-group flag; never **logic**.
- **BUILT + PUSHED `32b4eead` (08-19) — Todd: "the answer is very simple, just show the boolean col here."** One entry in `AGEGROUP_COLUMNS` (`ladt-grid-columns.ts`), 85px, matching the identical column the **Team** grid already carried. No backend, no DTO, no regen — `AgegroupDetailDto` already ships `bAllowSelfRostering` and the sibling grid's boolean+headerTooltip branch already renders this shape (3rd Party is the precedent). The ⓘ states the real rule (OR'd, not a cascade; window still gates). `npx tsc --noEmit` clean. Verified by Todd on `stepsgirls-players-2025-2026`.
- **Parts 2 and 3 deliberately NOT built — Todd's ruling that part 1 is the whole answer.** No Team-level ⓘ, no Active-vs-Self-Roster gloss; the Team grid already shows both columns side by side. **h-scroll ACCEPTED (Todd, 08-19): "3rd party pushing off is perfectly fine"** — Ann's no-h-scroll constraint is overridden, no donor column was shrunk (M/F and 3rd Party are AM-038-tuned; `Fees` at 220px is the slack if it's ever revisited).
- **Status**: ✅ VERIFIED PASSING (Ann, 08-21) — **CLOSED.** SELF ROSTER column confirmed on the Age Group grid; deploy evidently landed (she saw it). Parts 2–3 stay unbuilt per Todd's 08-19 ruling, and the h-scroll override stands — both acknowledged in the same pass.

### AR-005: [Coach Registrations / Security] SuperUser login was able to self-register as a Coach — intended?
- **Topic**: Coach Registrations → role-lane purity
- **Observation (Ann)**: Using the **TSIC SuperUser login**, Ann was able to **self-register as a Coach** on a job. Flagging because it was unexpected.
- **Question for Todd**: Is this intended? Two angles:
  1. **Testing convenience** — SuperUser is deliberately exempt from most gates (cross-job ops), so being able to register-as-anything may be by design for testing.
  2. **Lane-purity risk** — but a real Coach/Staff registration on a SuperUser account writes a live role grant. Per the AM-004 lane model, only LIVE registrations classify an account's eligibility; a stray Staff row on the SuperUser account could muddy that, or surface the SuperUser in coach/roster views. Confirm this doesn't poison the SuperUser's lane or leak it into director-facing lists.
- **For Todd**: decide whether SuperUser should be **blocked** from self-registering as a Coach (clean-lane), or **allowed** (testing) — and if allowed, confirm the resulting Staff row is filtered out of coach/roster/approval surfaces the way other admin lanes are.
- **Severity**: Question / possible security-lane concern (non-blocking; verify before go-live if it affects real accounts)
- **CONFIRMED TRUE (Claude, 08-19) — the behavior is real, no guard exists.** `POST /api/adult-registration/register-existing` is `[Authorize]` and nothing more (`AdultRegistrationController.cs:105`) — no role restriction, no admin-account exclusion. The role is resolved purely from job type (`AdultRegistrationService.ResolveCoach`, :1584) and real Registration rows are written against the SuperUser's own user id: an **UnassignedAdult** row on a Club site, or **one Staff row per team** (on the real roster, with the per-team fee cascade stamped) on Tournament/League.
- **NOT a security problem — ruled (Todd, 08-19): "why do we need to do anything?"** The direction is **downward**: reaching this endpoint requires already being authenticated as SuperUser, an account that already exceeds everything Staff grants. **No privilege escalation, no attacker, no customer-visible exposure.** What it actually produces is lane pollution with a money artifact — SuperUser visible in a director's Coach Approvals queue (the queue query filters role + active only, no admin exclusion), on a live team roster, holding a "Staff" login option, and carrying a coach fee. Every one of those is self-inflicted by TSIC staff, rare, and reversible by hand (deny the row, delete the roster row, void the fee). A guard would be new code on a live production auth path defending against our own testing. **WILL NOT DO. Do not re-propose the SuperUser block.**
- **Where the real exposure sits, if it is ever revisited — and it holds:** on Tournament/League with coach registration open, **any member of the public can self-place as Staff on ANY team in the event** — `BuildAndAddRegistrationsAsync` validates only that the team belongs to the job, never that the registrant has any relationship to it, and anonymous `RegisterNewUser` reaches the same code. That is the deliberate 2026-08-14 two-regime design (the club vouches; the director can't vet strangers). Verified the control is real: `MyRosterService.cs:37-63` gates on **`BAllowRosterViewAdult`** (default false, clone-reset to false, consent-gated in the Teams tab), restricts to the caller's **own** `AssignedTeamId`, and refuses system-bucket teams. **Trigger to re-examine: a live job running with `BAllowRosterViewAdult = true`.** Nothing else here warrants attention.
- **Status**: ✅ CLOSED — Won't Fix (Todd, 08-19). Behavior confirmed real; not a security issue; no guard added. ✅ Acknowledged (Ann, 08-21).

### AR-004: [Rosters / PDF] Roster PDF truncates the Position column — "Midfield" prints as "Midfiel"
- **Topic**: Rosters → PDF export → Position column
- **Observation (Ann)**: In the generated **roster PDF**, the **Position** value is **cut off** — e.g. "Midfield" renders as "Midfiel" (last character clipped). The column is too narrow / the cell text is being truncated rather than fit.
- **For Todd**: widen the Position column (or shrink/auto-fit the text) in the roster PDF generator so full position names print. Check the longest expected values (Midfield, Attack, Defense, Goalie, Long Stick Midfield / LSM, etc.) so the fix holds for the widest label, not just "Midfield". Likely a fixed column width or a text-clip in the PDF layout, not a data problem (the stored value is complete).
- **Severity**: Bug / output correctness (non-blocking; cosmetic-but-wrong on a printed artifact)
- **BLOCKED — WE DO NOT KNOW WHICH REPORT (Todd, 08-17): "not approachable until you know which report, you could really cause problems with packed reports."** Do NOT touch a generator on inference. **NEXT STEP: ask Ann which roster export she ran.**
- **Investigation banked (Claude, 08-17) — four generators have a Position column, each with its own width mechanism:**
  - `PackedRosterPdfService` — label literally **"Position"**, proportional `DefaultWidthWeight = 32`, `SupportsLongText = false` (:49). **HIGHEST RISK to change** — dense legacy-parity export family (PackedByPosition / by-School siblings); widening steals width from neighbours across every variant.
  - `MyRosterPdfService` — header **"Pos"**, fixed `30f` (:95). Would clip far harder than one character.
  - `ClubRosterPdfService` — no Position column at all; `DrawClip`s Position onto line 2 of the **DOB** column's width (:258).
  - `RosterTablePdfService` — configurable `"position"` key (:346); width comes from the caller's column set.
  - **None of them measure the text — they all clip.** Symptom of exactly one character lost out of eight *suggests* Packed Roster, but that is inference from a screenshot, not proof, and it is precisely the one that must not be changed on a guess.
  - **`SupportsLongText = false` is a deliberate flag, not an oversight** — so "widen" and "wrap" are two genuinely different fixes (wrap = variable row heights on a dense report).
  - **Size to the longest value actually in the data, not to "Midfield"** — `Long Stick Midfield` is far wider, so a fix eyeballed against Midfield leaves the bug live for LSM. Same approach settled in AM-038 (size to the longest real value, don't eyeball).
- **✅ UN-BLOCKED — REPORT IDENTIFIED (Ann + Claude, 08-21). It is `MyRosterPdfService`, NOT Packed Roster.**
  - **Ann's location**: "the roster that Coach has access to once approved" → the teammate roster card view (Find a teammate… / **Download PDF** / Email Selected / Email All) → **Download PDF**. Caller chain confirmed: `MyRosterService` → `IMyRosterPdfService` (`Program.cs:388`).
  - **Proof, not inference — three independent matches** against Ann's PDF screenshot:
    1. Header reads **`Pos`**, not `Position`. Packed Roster labels it `Position` in full; only `MyRosterPdfService` uses the abbreviation (`:95`).
    2. Column set is byte-for-byte `BuildColumns()` (`:91-101`): `# · Player · Pos · Grad · Email · Phone · Contact 1 · Contact 2`.
    3. Clip lengths fit the 30f width exactly — `midfield`(8)→`midfiel`, `defense`(7)→`defens`, `attack`(6) intact.
  - **⚠️ CORRECTION to the 08-17 banked investigation**: it reasoned "one character out of eight *suggests* Packed Roster." **That inference was wrong.** Packed Roster needs no change for this bug, and Todd's refusal to act on the guess is exactly what prevented a needless edit to the highest-risk generator. Leave `PackedRosterPdfService` alone.
- **Target (single line)**: `MyRosterPdfService.cs:95` — `new Col("Pos", 30f, PdfTextAlignment.Center, false, …)`. The `false` is `Wrap`, so the cell clips rather than fits.
  - **Widths are proportional weights, normalized** (`:37-38`, `c.Width / sumW * contentW`). Sum = 734 over a landscape-Letter 0.4in content width (~734pt), so today the weights ≈ points 1:1 — but **any increase to `Pos` shrinks every other column**, it is not free space.
  - **Donor columns**: `Contact 1` and `Contact 2` at **157f each** are by far the fattest and both already `Wrap: true`, so they absorb a trim by growing taller, not by clipping. `Email` (138f) is the next candidate. Do **not** raid `#`(26f) / `Grad`(34f) / `Phone`(72f).
  - **Widen vs wrap**: unlike Packed Roster, this report has **no `SupportsLongText = false` constraint** — `MeasureRowHeight` (`:156-170`) already computes per-row height from wrapped cells, so flipping `Pos` to `Wrap: true` is genuinely available here and costs only row height on long values.
- **⚠ STILL REQUIRED before sizing — query the real data.** Ann's sample tops out at `midfield` (8). **Do NOT size to that.** `Long Stick Midfield` (19) would still clip at any width that merely fits "midfield". Pull `SELECT DISTINCT Position` across live registrations, size to the longest actual value (or wrap), per the AM-038 precedent already cited above. Stored values appear **lowercase** in the PDF (`midfiel`, `attack`, `defens`) — confirm whether that is the stored casing or a render-time transform.
- **Status**: 🔴 OPEN — for Todd. Un-parked 08-21; report identified, target line known, data query outstanding.

### AR-003: [Coach Registrations] "Teams You're Coaching" list is hard to read — stack it vertically with a light (blue?) badge per team
- **Topic**: Coach Registrations → adult registration wizard → "Teams You're Coaching" review section (direct-placement Staff on Tournament/League)
- **Applies to**: **Direct-to-roster** coach type (tournaments, leagues) — the step showing the teams the coach will be placed on, above the "Submitting registers you as staff on **every team listed above**" callout.
- **Request (Ann)**: The teams listed under **"TEAMS YOU'RE COACHING"** are **not easily seen** — they currently run **left-to-right inline** on one line, which reads as a run-on. Make each team stand out:
  1. Render as a **vertical list** (one team per line, not a horizontal left→right row).
  2. Give each team a **light badge** (blue suggested) so they're visually distinct and scannable.
  - See attached image: two teams shown inline (`3 Point:2028:Unassigned:3 Point 2028   214LAXDALLAS:2028:Unassigned:214LAXDALLAS 2028`) with the amber "every team listed above" callout beneath.
- **For Todd**: this is the direct-placement review list (the one gated by the server-derived `DirectPlacement` flag from `e6d3bd08`). Use design-system tokens for the badge (no hardcoded blue — e.g. `.bg-primary-subtle` / a subtle info badge), keep the amber callout as-is.
- **Severity**: UI / readability (non-blocking)
- **FIXED (Claude, 08-17) — awaiting Ann verify.** CSS only, three declarations on `.teams-wrap` in `review-step.component.ts`: `flex-direction: column` + `align-items: flex-start` (each pill hugs its own text rather than stretching the column) + gap `--space-1` → `--space-2`. No template, binding, logic, or colour change.
  - **The badge Ann asked for already existed** — `.review-team-pill` is already primary-tinted background + primary border + primary text, all token-derived. The pills simply weren't stacked: `flex-wrap: wrap` at a 4px gap put two long `Club:Age:Division:Team` labels side by side, which is the run-on she saw. Nothing was added for the badge.
  - **Shape precedent:** the Profile step one screen earlier already renders this same selection as a vertical one-per-line list — Review was the odd one out. Matched its *arrangement*, kept Review's *styling*; did NOT convert to Profile's `<ul>`, which would be a template change and would drop the badge.
  - **Both faces of the section move, intended:** direct-placement "Teams You're Coaching" and the club-site "Teams Requested" `@else` branch share `.teams-wrap`. Both were inline; both now stack.
  - **Risk:** nil. Angular view encapsulation scopes the rule to this component; `.teams-wrap` exists nowhere else (the class *name* `.review-team-pill` is also used by the player review step, but as an independent scoped copy — untouched either way). No colour declarations, so every palette and both light/dark render as before. No animation, so nothing for reduced-motion. `npx tsc --noEmit` clean.
- **NOT DONE, deliberately — the label noise.** `3 Point:2028:Unassigned:3 Point 2028` repeats the club and year and leaks the literal **"Unassigned"** division placeholder into customer-facing text. It comes from `availableTeams().displayText`, the SAME value feeding the ej2 team picker one step earlier. Rejected both fixes: a Review-only transform would print one team under two different names three clicks apart in one wizard (worse than a noisy label), and changing `displayText` at the source moves the picker — not no-risk, and not what Ann filed. **File separately if the placeholder leak matters.**
- **Status**: ✅ VERIFIED PASSING (Ann, 08-21) — **CLOSED.** Stacked one-per-line list confirmed on the Review step. The label-noise follow-up above (`Club:Year:Unassigned:Team` repetition) remains unfiled — raise it as its own item if it matters.

### AR-002: [Coach Registrations] Tournament/league coaches register directly as Staff (never Unassigned Adult) — confirm behavior + clean up residual Unassigned Adults
- **Topic**: Coach Registrations → Staff vs Unassigned Adult
- **Applies to**: **Direct-to-roster** coach type (tournaments, leagues). Ann believes this pertains to the **upcoming Fall tournaments** specifically.
- **Observation (Ann)**: On tournament/league sites, a Coach is registered **directly as Staff** and **never passes through the Unassigned Adult state**.
- **Confirm (for Todd)**: Is this the intended behavior — direct-to-Staff, no Unassigned Adult step?
- **If confirmed, two follow-ups**:
  1. **Director-account creation** — does this mean tournament/league sites **cannot be used to create a Director account** (since a Director account path presumably runs through the Unassigned Adult → elevate flow)? Confirm true/false. *(Ties to AR-001's "register as Coach en route to Director" case — if direct-to-Staff is the rule, that AR-001 rationale may not hold.)*
  2. **Residual cleanup** — there are **leftover Unassigned Adult registrations from before the release** on these jobs. Were they all **approved**? If so, should we **delete the now-distinct Unassigned Adult registrations** so directors aren't confused by duplicate/orphaned adult rows?
- **Severity**: Question + data cleanup (non-blocking; verify before deleting anything)
- **Data check available**: Claude can enumerate the residual Unassigned Adult registrations on the upcoming Fall tournament jobs (count + per-job breakdown) so the cleanup decision is made against real numbers, not an estimate.
- **CONFIRMED (Claude, code + data, 2026-08-14 code):** The direct-to-Staff behavior is **intended and current**. Todd's commit `e6d3bd08` ("two-regime coach registration") makes Tournament/League coach self-reg resolve to **`RoleConstants.Staff`** with **one Staff Registration per selected team** (binding, no approval, no UnassignedAdult row). Club (player-reg) sites keep the UnassignedAdult funnel + Roster Swapper approval. Live data corroborates: Fall 2026 tournaments are Staff-only, zero UnassignedAdult (e.g. `Karenpequa@aol.com` = 7 Staff rows minted in the same instant = one per team).
  - **So follow-up #1 (Director-account path):** on Tournament/League there is no UnassignedAdult step at all, which reinforces AR-001's question — a coach on these sites lands as Staff, not as an elevatable Unassigned Adult. Todd to confirm whether the "register as Coach → become Director" path is expected to exist on team-reg sites (it may only exist on Club sites).
  - **So follow-up #2 (residual cleanup) — two DISTINCT cases:**
    - **Club sites:** already handled — commit `75d82744` **deletes the UnassignedAdult row when a director grants a pending coach**, so no residual accumulates going forward.
    - **Tournament/League sites:** the grant-delete does **not** apply (there's no grant step here — coaches are placed directly). Any UnassignedAdult rows on these jobs are **pre-release residue** from the old universal-funnel behavior, orphaned by the regime switch. **This is the case that still needs a one-time cleanup decision** — enumerate + confirm none are legitimately in-flight, then delete so directors don't see phantom Unassigned Adults next to the real Staff placements.
- **RULING — FULLY RESOLVED, no code change (Todd, 08-19). CLOSED; do not re-raise either half.**
  - **(a) Director-account path — club-site-only, by design.** `AdministratorService.AddAdministratorAsync` accepts exactly two shapes: a **lane-pure admin account** (already Director/SuperDirector somewhere), or **exactly one active Unassigned Adult row on any job of any customer**. A **Staff** row qualifies as neither — so a tournament/league coach registration cannot seed a Director account. The person must register as a coach on a **Club** site (cross-customer is allowed), then be accepted here. Accepted as correct: Staff is a binding roster hat, not a vetting artifact.
  - **Known, accepted consequence — the empty-search copy loops on team-reg sites.** `admin-form-modal.component.ts:151` (`outsideLane`) tells the director *"Register the person as a coach (any event site), then accept them here"*; on a tournament, obeying that mints Staff again, forever. **Only bites a person with no prior admin role anywhere** — an existing Director elsewhere is added normally. Left as-is by ruling; file a fresh item only if a director actually hits it.
  - **(b) Residual UA cleanup — not pursued.** The pre-release Unassigned Adult rows on tournament/league jobs stay where they are. No enumeration was run, nothing deleted. Club side remains covered by `75d82744`.
- **Diagnostic fact worth reusing:** "tournament/league" is **not one bucket** for adult roles. The `unassigned` role key is **rejected outright on Tournament** but **still valid on League** (`AdultRegistrationService.cs:1509`), so a league can legitimately still mint an approval-queue row — even though the app's own links route league coaches to the `coach` key → Staff (`translate-legacy-urls.pipe.ts:213`). Bears directly on AR-001.
- **Status**: ✅ CLOSED (Todd, 08-19) — behavior CONFIRMED and intended; both follow-ups ruled, no work taken. ✅ Acknowledged (Ann, 08-21).

### AR-001: [Coach Registrations] Coach Approval menu still shows for tournaments/leagues (direct-to-roster) — keep or hide?
- **Topic**: Coach Registrations → Coach Approval menu
- **Applies to**: **Direct-to-roster** coach type (tournaments, leagues) — where coaches are placed straight onto the team roster and the approval step isn't used.
- **Observation (Ann)**: The **Coach Approval** menu is still present for tournament/league sites, even though those sites place coaches directly on rosters and won't use the approval workflow.
- **Ann's leaning**: Initially thought we should **hide it** on these site types. But there may be a legitimate reason to keep it: someone could register as a **Coach** on this type of site **as part of the process of being added as a Director** — in which case the approval menu could still be useful.
- **Question for Todd**: Should the Coach Approval menu be **hidden** on direct-to-roster (tournament/league) sites, or **left visible** to support the "register as Coach en route to Director" case? Decision his call.
- **Severity**: Question / product decision (non-blocking)
- **RULING — WILL NOT DO (Todd, 08-19): "ignore it, not a problem."** The menu stays visible on every job type. No nav visibility rule, no code, no Nav Editor change. **Do not re-propose hiding it.**
- **Findings banked (Claude, 08-19), so this isn't re-derived:**
  - Ann's "register as Coach en route to Director" rationale is **dead** — settled by the AR-002 ruling: a tournament/league coach registration mints **Staff**, which never reaches this queue, and the Director path is club-site-only. The menu is NOT being kept for that reason; it's kept because the cost of the empty screen is nil.
  - The screen is **not guaranteed empty** anyway: the queue is every active Unassigned Adult row on the job (`RegistrationRepository.GetUnassignedAdultQueueAsync`, :3152), and the pre-release residue (AR-002b, ruled to stay) still shows there — plus on **League** the `unassigned` key remains a live carve-out. **Tournament** is the only type where the platform categorically refuses to mint a new row.
  - Had we hidden it, it would have been a **data** change, not code: the nav row seeds with `VisibilityRules = NULL` (`scripts/5) Re-Set Nav System.sql:139`) and the engine already supports a `jobTypes` **allowlist** (`visibility-rules.ts:81`), settable by a SuperUser in **Configure → Nav Editor** — the same mechanism Check-In and Camp Groups use. Allowlist-only, so "hide on Tournament" means listing the other five types.
  - **Second door, ungated:** the Roster Swapper header carries its own **Coach Approvals** button with no job-type check (`roster-swapper.component.html:7`). Any future attempt to hide this screen must gate that too, or the door dangles. Moot under this ruling.
- **Status**: ✅ CLOSED — Won't Fix (Todd, 08-19). Menu stays visible everywhere. ✅ Acknowledged (Ann, 08-21).
